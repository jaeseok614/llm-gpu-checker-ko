{
  "name": "ai-hardware-fit",
  "version": "1.5.0",
  "private": false,
  "description": "Bilingual static web app for estimating LLM, retrieval, vision, image, video, STT, and TTS workloads on GPUs.",
  "type": "module",
  "bin": {
    "ai-hardware-fit": "./scripts/benchmark-cli.mjs"
  },
  "scripts": {
    "check": "node --check app.js && node --check scripts/benchmark-cli.mjs && node --check scripts/gpu-request-tools.mjs && node --check scripts/model-request-tools.mjs && node --check scripts/benchmark-request-tools.mjs && node scripts/validate-data.mjs && node scripts/build-static.mjs && node --test tests/*.test.mjs",
    "test": "node --test tests/*.test.mjs",
    "benchmark": "node scripts/benchmark-cli.mjs",
    "social-preview": "node scripts/generate-social-preview.mjs",
    "gpu-request:validate": "node scripts/gpu-request-tools.mjs",
    "model-request:validate": "node scripts/model-request-tools.mjs",
    "benchmark-request:validate": "node scripts/benchmark-request-tools.mjs",
    "build:static": "node scripts/build-static.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  },
  "keywords": [
    "llm",
    "gpu",
    "vram",
    "rag",
    "embedding",
    "reranker",
    "ocr",
    "image-generation",
    "video-generation",
    "speech-to-text",
    "text-to-speech",
    "korean",
    "hardware"
  ],
  "license": "MIT"
}
