{
  "name": "ai-hardware-fit",
  "version": "1.3.0",
  "private": false,
  "description": "Bilingual static web app for estimating LLM, embedding, reranker, OCR, VLM, image, and video generation workloads on GPUs.",
  "type": "module",
  "bin": {
    "ai-hardware-fit": "./scripts/benchmark-cli.mjs"
  },
  "scripts": {
    "check": "node --check app.js && node --check scripts/benchmark-cli.mjs && node --check scripts/gpu-request-tools.mjs && node --check scripts/benchmark-request-tools.mjs && node scripts/validate-data.mjs && node scripts/build-static.mjs && node --test tests/*.test.mjs",
    "test": "node --test tests/*.test.mjs",
    "benchmark": "node scripts/benchmark-cli.mjs",
    "social-preview": "node scripts/generate-social-preview.mjs",
    "gpu-request:validate": "node scripts/gpu-request-tools.mjs",
    "benchmark-request:validate": "node scripts/benchmark-request-tools.mjs",
    "build:static": "node scripts/build-static.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  },
  "keywords": [
    "llm",
    "gpu",
    "vram",
    "rag",
    "embedding",
    "reranker",
    "ocr",
    "image-generation",
    "video-generation",
    "korean",
    "hardware"
  ],
  "license": "MIT"
}
