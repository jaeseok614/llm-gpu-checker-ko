# Changelog

## Unreleased

- **가이드 창의 "AI가 쓴 것처럼 보이는" 문구와 예시 버튼 줄바꿈 정리:** "GPU는 있는데 뭘 돌릴 수 있는지 궁금하다면" 등 각 안내 항목 오른쪽에 있던 "입력 1개 · 약 10초" 같은 시간·단계 수 태그가 사람이 직접 쓴 것 같지 않고 AI가 자동 생성한 듯한 느낌을 준다는 지적을 받아 완전히 제거했습니다(4개 항목 전부, 한/영 모두). 또한 하단 "예시로 보기" 줄에서 4번째 칩("Llama 70B+임베딩 2장 배치")만 다음 줄로 밀려 어색해 보이던 것을, 줄바꿈 대신 한 줄을 유지하고 필요하면 가로 스크롤되도록 고쳤습니다.

## v7.20.3 - 2026-08-31

- **헤더의 "가이드" 버튼을 눌러도 처음 사용 가이드 창이 아예 뜨지 않던(깨지던) 버그 수정:** GPU를 이미 한 번이라도 선택한 상태(거의 모든 재방문 사용자의 기본 상태)에서는 `#coreTaskSwitcher`에 `is-collapsed` 클래스가 붙는데, `styles/v6-guided.css`에 남아있던 예전(가이드 버튼이 헤더로 옮겨지기 전) 규칙 `.core-task-switcher.is-collapsed .getting-started-panel { display: none; }`이 특이도(specificity)상 항상 이겨서, 가이드 버튼 클릭 시 JS는 정상적으로 `hidden` 속성을 지우는데도 이 규칙이 계속 `display: none`을 강제해 가이드 창이 절대 보이지 않았습니다. 같은 자리에 남아있던, 이제는 존재하지 않는 `.core-task-intro`/`.core-task-demos`, 그리고 이제 푸터로 옮겨져 더 이상 자식 요소가 아닌 `.catalog-request-center`를 겨냥한 죽은 규칙도 함께 정리했습니다. 추가로, 같은 `is-collapsed` 상태에서 활성 탭에 `width: 100%`를 강제하던 규칙도 발견해 제거했습니다 — 이 역시 실제로 살아있던 규칙이라 v7.20.1에서 고친 "탭 하나가 부풀어 나머지가 밀려나는" 문제를 GPU가 이미 선택된 상태(즉 거의 항상)에서 다시 일으킬 수 있었습니다.

## v7.20.2 - 2026-08-31

- **영어 모드에서 벤치마크 표의 "Environment" 열 값이 한글로 남아있던 버그 수정:** "Environment" 등 표 헤더는 영어로 잘 나오는데, 각 행의 실제 값(예: "공식 품질", "공식 발표")은 계속 한글로 나온다는 제보를 받았습니다. 원인은 벤치마크 화면(`features/benchmark-workspace.js`)이 지연 로딩(스크롤로 화면에 들어오거나, 2.5초 타임아웃, 지표 필터 클릭 시 로드)되는데, `feature-loader.js`의 로딩 완료 콜백이 표를 렌더링만 하고 번역 스윕(`translateDynamicUi`)은 호출하지 않았기 때문입니다. 언어 토글 버튼을 누르거나 인프라/배치/모델 찾기/API 비용 모드로 들어갈 때는 이미 이 스윕이 따라붙지만, 처음부터 영어 모드로 페이지를 열고 벤치마크 화면을 처음 스크롤해 들어오는 경우에는 이 스윕이 한 번도 실행되지 않아, `qualityBenchmark.note`처럼 언어 조건 없이 한글이 그대로 박혀 있는 데이터 필드가 영원히 한글로 남았습니다. `feature-loader.js`의 벤치마크 지연 로딩 콜백에도 같은 스윕을 추가했고, 회귀 테스트도 추가했습니다.

## v7.20.1 - 2026-08-31

- **탭 바를 다시 전체 폭으로 바꾸자 탭 하나가 부풀어 나머지가 화면 밖으로 밀려나던 문제 수정(v7.20.0 수정 직후 발견):** `#coreTaskSwitcher`를 `width: fit-content`에서 `width: 100%`로 되돌린 것 자체는 의도대로 작동했지만(jsdom 캐스케이드 점검에서는 문제 없음으로 나옴), 실제 브라우저에서는 활성 탭 하나가 줄 전체를 부풀려 차지하고 나머지 5개 탭이 화면 오른쪽 밖으로 밀려나 아예 안 보이는 문제가 발생했습니다(사용자 스크린샷으로 확인). `.core-task-actions`가 `width: auto` 상태로 세로 방향 스위처의 `align-items: stretch`에 기대어 폭을 채우고, 버튼들은 `flex: 0 0 auto`(내용 크기 그대로, "shrink-to-fit" 방식)로 되어 있던 조합이 실제 브라우저의 내재적 크기 계산에서 문제를 일으킨 것으로 보입니다. `.core-task-actions`에 `width: 100%`를 직접 명시하고, 버튼들은 `flex: 1 1 auto`(내용 비율대로 균등하게 늘어나는 표준 방식)로 바꿔 내재적 크기 계산에 의존하지 않도록 고쳤습니다.

## v7.20.0 - 2026-08-31

- **더보기▾ 메뉴를 없애고 6개 탭을 모두 펼쳐서 보여주도록 변경:** 더보기▾ 안에 항목이 "AI 스택 배치"와 "API 비용 계산기" 딱 2개뿐이라 굳이 드롭다운으로 숨길 필요가 없다는 지적을 반영해, 두 항목을 GPU→Model / Model→GPU / Infrastructure / Benchmarks 옆에 똑같은 모양의 일반 탭으로 나란히 배치했습니다(총 6개 탭). 관련해서 더 이상 쓰이지 않는 더보기▾ 토글·드롭다운 열고 닫는 JS, i18n 문구, CSS를 함께 정리했습니다.
- **탭 바 박스 폭을 아래 패널과 맞춤:** v7.19.0~v7.19.1에서 탭 바 박스를 `width: fit-content`로 줄였던 것이, 탭이 6개로 늘어난 지금은 오히려 아래 하드웨어/결과 패널과 좌우 여백(폭)이 맞지 않아 보인다는 지적을 받았습니다. 탭 줄이 이제 한 줄로 압축되어 있어 예전처럼 박스가 크고 어색해 보이지 않으므로, 박스를 다시 `width: 100%`로 되돌려 아래 패널들과 좌우 끝이 정확히 맞도록 했습니다.

## v7.19.1 - 2026-08-31

- **탭 바가 여전히 세로로 부풀어 보이던 진짜 원인을 찾아 수정(v7.19.0 수정으로도 남아있던 문제):** v7.19.0에서 폭 문제(오른쪽 빈 공간)는 해결됐지만, 확인해보니 활성 탭 하나가 줄 전체를 차지하며 세로로 크게 부풀고 나머지 탭들이 그 아래 줄로 밀려 내려가는 새로운 증상이 나타났습니다. 원인을 다시 추적한 끝에, 이 파일에 아주 오래전(탭 바가 가로 배치였던 시절)부터 남아있던 `.core-task-switcher > div:first-child { flex: 0 1 310px; }` 규칙을 찾았습니다 — 그때는 "폭 310px"이었지만, 지금은 탭 바 컨테이너가 세로 방향(`flex-direction: column`)으로 바뀌어 있어 같은 310px이 탭 버튼 줄(`.core-task-actions`, 스위처의 첫 자식 div)의 "높이" 기준으로 적용되고 있었고, 그 늘어난 높이를 버튼들이 `align-items: stretch`로 그대로 따라가면서 매번 다시 부풀어 보였던 것입니다. `#coreTaskSwitcher .core-task-actions`에 `flex: 0 0 auto`를 명시해 이 옛 규칙을 무력화했고, 탭 줄 자체도 줄바꿈 없이 한 줄에 자연스러운 폭으로 놓이도록 정리했습니다(680px 이하 모바일 폭에서는 기존대로 세로로 쌓이는 동작 유지).

## v7.19.0 - 2026-08-31

- **탭 바 실사용 화면 문제 2건 수정(v7.18.0에서 그대로 남아있던 것을 사용자 스크린샷으로 확인):** 새로고침 후에도 맨 위 작업 선택 탭이 (1) 오른쪽에 큰 빈 공간을 남긴 채 왼쪽으로 치우쳐 보이고 (2) 위아래로 여전히 길다는 지적을 받아 확인했습니다. 원인은 `#coreTaskSwitcher`가 `.app-shell`의 그리드 한 칸을 그대로 채우도록(`justify-items: stretch`) 늘어나 있어 버튼이 필요로 하는 폭보다 훨씬 넓은 박스가 되어 있었던 것, 그리고 v7.18.0에서 일반 클래스 선택자(`.core-task-button` 등)로 넣은 압축 스타일이 같은 파일 안의 더 오래된 v6 시절 규칙과의 상호작용에서 실제 브라우저 렌더링 기준으로는 기대만큼 이기지 못하고 있었던 것이었습니다(자세한 원인 규명보다 확실한 해결을 위해 `#coreTaskSwitcher` ID로 범위를 좁혀 우선순위로 확실히 이기도록 다시 작성). `#coreTaskSwitcher`에 `width: fit-content`를 지정해 박스가 버튼 내용만큼만 차지하도록 하고, 버튼 크기·줄간격도 ID 선택자로 다시 고정했습니다.
- 이번 지적 과정에서, 사용자가 처음 보고한 "레이아웃이 그대로다"는 실제로는 배포 자체의 문제가 아니라 브라우저 캐시 문제였음을 강력 새로고침으로 확인했습니다(콘솔에는 무관한 `favicon.ico` 404 하나만 남아 있었음).

## v7.18.0 - 2026-08-31

- **첫 화면 밀도 조정(사용자 스크린샷 피드백 반영):** v7.17.0 레이아웃 재정리 이후 받은 피드백 중 "레이아웃 완성도"에 관한 지적 4건을 우선 반영했습니다.
  - 맨 위 작업 선택 탭(GPU→Model / Model→GPU / Infrastructure / Benchmarks / More▾) 박스의 패딩·줄간격을 줄여 더 슬림하게 만듦.
  - "현재 단계" 진행 표시줄에 있던 "다른 작업 선택" 버튼 삭제 — 위의 탭 바 자체가 항상 보이는 전환 수단이라 별도 버튼이 중복이었음(내부적으로도 이 버튼이 토글하던 `is-collapsed`/`guided-workspace-started` 클래스는 이미 어떤 CSS도 참조하지 않는 죽은 상태였음을 확인).
  - GPU 패널의 "AI 스택 배치" 버튼 삭제 — 더보기▾ 메뉴의 "Stack Planner" 항목이 이미 완전히 동일한 동작(`openPlacementPlanner([], { showBuilder: false, seedHardware: true })`)을 호출하는 중복 버튼이었음.
  - "이 GPU로 할 수 있는 작업" 카드의 패딩·줄간격을 줄여 높이를 약 25% 축소.

## v7.17.0 - 2026-08-31

- **첫 화면~추천 결과 UI 계층 재정리(기능 삭제 없음):** "지금 사이트가 AI 티 나는 정도"라는 지적을 받아, 헤더부터 GPU 추천 결과·GPU 비교까지(벤치마크 섹션과 계산 근거 푸터는 이번 범위에서 제외) UI 구조만 다시 짰습니다. 기능은 하나도 지우지 않고, 처음 방문 시엔 안 보여도 되는 안내성 요소를 뒤로 옮기거나 접었습니다.
  - 숫자가 붙은 4단계 온보딩 카드("1. GPU 선택 → 2. ...")를 아이콘+한 줄 설명의 평면 탭 바로 교체하고, "30초 시작"/"60초 체험" 같은 소요 시간 문구를 전부 제거. 배치·API 계산기는 "더보기▾" 메뉴로 이동.
  - "처음 사용 가이드"를 본문에서 헤더의 버튼 하나로 옮기고, 그 안에 있던 "60초 체험" 데모 예시들도 가이드 패널 쪽으로 재배치.
  - GPU 선택 영역을 "GPU 변경" 토글 뒤로 접어 기본은 요약 한 줄만 보이게 하고(이미 GPU를 고른 뒤에만 나오는 패널이라 최초 방문 흐름에는 영향 없음), 실제 프리셋을 고르면 자동으로 다시 접히도록 함.
  - 추천 결과 헤더의 중복된 "현재 GPU" 박스를 제거하고 한 줄 요약으로 통합, 결과 화면의 액션 버튼들(링크 복사/카드 PNG 다운로드)을 "공유▾" 드롭다운 하나로 정리(고정 노출되던 RTX 3060 예시 링크는 삭제), GPU 비교 섹션 제목도 부제 없이 한 줄로 축소.
  - GPU/모델 추가 요청 센터와 피드백 링크는 푸터로 이동. 생성형 LLM 상세 설정(컨텍스트·KV 캐시·양자화·런타임)과 벤치마크 섹션, 계산 근거 푸터는 이번 정리에서 그대로 유지.
- **"API 비용 계산기"를 "API vs Local"로 개편:** 제공사 체크박스 대신 품질 등급(플래그십/균형형/저가형)을 1차 선택지로 바꾸고, 등급에 맞는 모델 3개(제공사별 1개씩)를 기본으로 보여준 뒤 "전체 9개 보기"로 펼치도록 변경. "용도"(범용/RAG/코딩/추론/배치) 선택지를 추가했지만 이는 등급 추천에만 영향을 주고 비용 계산식 자체는 바꾸지 않음. "해석 결과"라는 AI 해석처럼 보이던 표현을 없애고, 입력값을 그대로 계산한 "비교 조건"(월 요청 수·입출력 토큰·총 토큰) 요약으로 대체. 기능 이름도 "API 비용 계산기"에서 "API vs Local"로 변경.
- **하드웨어 회수 기간·연차별 비교 추가:** "인프라 견적"의 자체 구축 vs API 비교에, 기존 손익분기 요청량 계산에 더해 하드웨어 구매 비용이 몇 개월 만에 API 대비 회수되는지("약 N개월 만에 회수됩니다" 또는 회수되지 않는 경우 그 사실을 그대로 안내)와 1~3년차 누적 비용 비교표를 추가.
- **품질 동등 오해 방지 문구 추가:** 최저가 API와 사용자가 설정한 로컬 모델의 품질이 같다고 오해하지 않도록, "동일 사용량 기준 '비용'만 비교한 값이며 모델 품질이 동일하다는 뜻은 아닙니다" 문구를 명시.
- **비용 외 비교 3가지 추가:** 초기 비용·데이터 외부 전송 여부·운영 부담을 비교하는 표를 API vs Local 비교에 추가(자유 텍스트 검색이나 자연어 "해석" 입력은 이번 범위에서 추가하지 않음).

## v7.16.0 - 2026-08-31

- 언어 전환(한/영) 시 여전히 한글로 남아있던 UI 잔여 버그 3건을 사용자 지적("한/영 바꿀때 한글로만 되있는게 있나")으로 라이브 사이트를 직접 확인해 찾아 수정: (1) "커뮤니티 데이터"(실측 결과 제보) 패널 전체가 영어 모드에서도 계속 한글로 남아있던 문제 — `renderWorkbench()`가 최초 1회만 그려지고 `setUiLanguage()`가 재호출하지 않던, GPU 인사이트/API 비용 패널에서 이미 고쳤던 것과 같은 버그 패턴. 사용자가 입력 중이던 텍스트영역 값은 보존하면서 라벨만 다시 그리는 `relabelWorkbench()`를 추가해 연결. (2) "처음 사용 가이드"/"빠른 추천 상세" 접근성 라벨(aria-label)이 "사용 가이드"/"빠른 추천"만 부분 치환되어 "처음 Quick guide"처럼 한/영이 섞이던 문제 — 전체 문구 사전 항목 추가로 수정(화면엔 안 보이고 스크린리더에만 영향). (3) 벤치마크 데이터가 아직 로드되기 전 첫 화면 placeholder("벤치마크 데이터 준비 중")가 영어 모드에서 "Benchmarks 데이터 준비 중"으로 반쪽만 번역되던 문제 — 전체 문구 사전 항목 추가로 수정. 3건 모두 회귀 테스트 추가
- "인프라 견적"의 "자체 구축 vs API" 비교에 손익분기점 설명과 사용량 배율(0.25x/1x/4x/16x) 비교표 추가 — 기존에는 현재 슬라이더 값 기준 스냅샷 한 줄만 보여줘 "동접(사용량)이 바뀌면 결과가 어떻게 달라지는지 잘 안 보인다"는 피드백이 있었음. API 비용은 사용량에 선형 비례, 자체 구축은 고정 월 비용이라는 점을 이용해 두 비용이 교차하는 지점(월 요청 수)을 계산해 "지금보다 약 Nx 늘어나면/줄어들면 역전됩니다" 문장으로 보여주고, 사용량 배율별 비교표로 실제 두 비용선이 벌어지는 모습을 같이 보여줌
- "API 비용 계산기" 표에 제공사(OpenAI/Anthropic/Google)·등급(플래그십/균형형/저가형) 필터와 열 헤더 클릭 정렬 추가 — "최저가" 배지는 필터와 무관하게 항상 추적 중인 9개 모델 전체 기준으로 계산해, 필터링된 결과 안에서만 비싼 모델이 "최저가"로 잘못 표시되는 일이 없도록 함
- 헤더 로고("내 GPU에서 돌아가는 AI 모델 찾기" 아이콘+제목)를 클릭 가능한 버튼으로 변경 — 일반적인 웹사이트의 "로고 클릭 시 첫 화면으로" 패턴과 동일하게, 클릭하면 기본 모드(GPU 찾기)로 돌아가고 작업 선택 화면을 다시 펼치며 맨 위로 스크롤함(입력했던 GPU/모델 선택값 자체는 지우지 않음)
- 버튼에 마우스를 올렸을 때 커서가 화살표 그대로였던 문제 수정 — 전역 `button` 스타일에 `cursor: pointer` 추가(비활성 버튼은 기존처럼 `not-allowed` 유지)

- r/LocalLLaMA 게시글 댓글 피드백 반영: (1) 언어를 영어로 바꿔도 브라우저 탭 제목이 한글로 남아있던 버그 수정, (2) 저장된 언어 설정이 없는 첫 방문자는 브라우저 언어(navigator.languages)를 감지해 한국어/영어를 자동 선택하도록 변경(둘 다 아니면 한국어 유지, 이미 선택한 적 있는 재방문자는 그 선택을 항상 우선), (3) AMD Ryzen AI Max+ 395 통합메모리 GPU에 32GB·96GB 구성 추가(기존엔 64GB·128GB만 있었음) — 기존 64GB/128GB 항목과 동일하게 GPU 계산 기준 VRAM은 75%(이미 이 저장소가 Apple Silicon/DGX Spark 등에 일관 적용 중인, 근거가 확인된 비율)로 채움, (4) GPU 검색창에 "Max"처럼 짧은 키워드를 입력하면 실제로는 "Ryzen AI Max+ 395"를 찾아낼 수 있는데도 "GPU를 찾을 수 없음" 안내가 함께 뜨던 불일치 수정 — 안내 문구의 판정 로직이 검색 로직보다 더 엄격한 완전일치만 확인하고 있었음, (5) IBM Granite 4.2(3B/8B/30B, reasoning 특화)와 Ant Group Ling 3.0 Flash(124B/활성 5.1B MoE) 모델 데이터 추가. 댓글에서 함께 지적된 "128GB 구성은 96GB가 아니라 122GB까지 VRAM으로 쓸 수 있다"는 부분은 이 저장소가 이미 별도로 재검증한 근거(Variable Graphics Memory/GTT 상한 75% 유지, 아래 항목 참고)와 상충해 반영하지 않음. "128GB Strix Halo에서 오래된 Mixtral 8x7B가 품질 1순위로 뜬다"는 지적도 재현을 시도했으나, 이미 추가돼 있던 Solar Open 2 250B·Qwen3-Next 80B·70B급 dense 모델들이 현재 데이터셋에서는 더 상위로 랭크되어 재현되지 않음(과거 버전 기준 관찰로 추정)
- "API 비용 계산기"를 5번째 핵심 기능으로 추가(고급 도구 메뉴에서 접근) — GPU를 사지 않고 OpenAI·Anthropic·Google의 호스팅 API를 그대로 쓸 때 예상 월 비용을 계산·비교. 각 제공사 공식 가격 페이지(openai.com/api/pricing, platform.claude.com/docs pricing, ai.google.dev/gemini-api/docs/pricing, 2026-08-28 확인)에서 가져온 실제 표준 요금(플래그십/균형형/저가형 각 1개씩, 총 9개 모델: GPT-5.6 Sol/Terra/Luna, Claude Opus 5/Sonnet 5/Haiku 4.5, Gemini 3.1 Pro Preview/3.7 Flash/3.5 Flash-Lite)만 사용하고, 프롬프트 캐싱·배치 할인·장문 컨텍스트 할증은 반영하지 않은 표준가 기준임을 명시. 로컬 GPU 카탈로그(data/models.js)와는 완전히 분리된 새 데이터 파일(data/api-models.js)로 관리해 VRAM 계산 로직에는 영향 없음
- "인프라 견적"(자체 구축) 결과 화면에 같은 사용량 가정(동시 요청·입출력 토큰·운영시간)을 그대로 재사용해 "자체 구축 vs API" 월 비용 비교를 추가 — 추천 구성의 3년 TCO를 월 환산한 값과, 추적 중인 API 중 가장 저렴한 모델의 예상 월 비용을 나란히 보여주고 API 비용 계산기 전체 비교로 바로 이동하는 버튼 제공
- 위 두 기능을 만드는 과정에서 `scripts/validate-data.mjs`/`scripts/data-audit.mjs`에 API 모델 스키마 검증과 90일 가격 노후화 경고(GPU 시세보다 훨씬 짧은 기준 — API 요금은 더 자주 바뀜)를 추가
- "60초 체험" 데모 버튼을 4번째로 추가 — "여러 모델 함께 배치"(고급 도구 안의 배치 플래너)를 안내 없이 바로 체험할 수 있도록, RTX 4090 24GB 2장 + Llama 3.1 70B Instruct + Qwen3-Embedding-4B를 미리 채워서 보여줌(실제 GPU 프리셋·모델 카탈로그 항목만 사용, 가상 데이터 없음)

- 이 데모 버튼을 만들다가 실제 프로덕션 버그를 하나 발견해서 함께 수정: GPU 배치 플래너(그리고 모델 파인더·인프라 스튜디오)에 임베딩 모델을 추가하면 "Qwen/Qwen3-Embedding-4B" 같은 모델명이 화면에 "Qwen/Qwen3-임베딩-4B"로 깨져서 나오던 문제. 원인은 `translateDynamicUi()`(영/한 단어 치환용 범용 사전)가 이 세 화면(배치/모델파인더/인프라)에서는 언어가 이미 한국어여도 매 렌더링마다 무조건 실행되는데, 사전에 있는 `["임베딩", "Embedding"]` 항목이 "Embedding"이 하이픈으로 구분된 독립 단어처럼 보이면 모델명 안이라도 그대로 갈아치웠기 때문(라틴 문자 경계 가드는 있었지만 하이픈/슬래시 경계는 막지 않음). 카탈로그의 실제 모델명과 텍스트가 정확히 일치하면 사전 치환을 건너뛰도록 고쳐, 앞으로 어떤 모델명에 "Embedding" 같은 사전 등재 단어가 들어가도 안전하게 방어. 회귀 테스트도 추가(`tests/smoke.test.mjs`)

- 영어 모드로 전환할 때 GPU 비교 상세 패널(아키텍처/메모리/대역폭/런타임 옆의 "데이터 완성도"·"사양 근거"·"검증일" 등)이 계속 한글로 남아있던 버그 수정 — 원인은 `setUiLanguage()`가 언어만 바꿀 때 이 패널을 그리는 `renderGpuInsights()`를 호출하지 않고 있었던 것. 짧은 단어(아키텍처/대역폭/런타임)는 범용 사전 치환(`translateDynamicUi`)에 이미 등록돼 있어 우연히 바뀌어 보였지만, 더 긴 문구들은 사전에 없어 그대로 한글로 남았음. `setUiLanguage()`가 언어 전환 시 `renderGpuInsights()`도 함께 다시 그리도록 고치고, 해당 문구들을 사전에도 보강해 이중으로 방어. 재발 방지용 DOM 회귀 테스트도 추가(`tests/smoke.test.mjs`)

- r/LocalLLaMA 커뮤니티 요청(u/Sudden-Guide)으로 AMD Radeon 780M(라이젠 7040/8040 "Phoenix"/"Hawk Point" 노트북·미니PC 내장 RDNA3 iGPU)을 GPU 카탈로그에 추가 — 전용 VRAM이 없어 시스템 RAM을 공유하는 구조라, cpu-monkey.com의 공식 스펙표(12 CU, iGPU 전용 최대 4GB 예약 가능, 통합 메모리 중 최대 32GB까지 사용 가능)를 근거로 32GB 통합메모리 구성으로 등록. 대역폭은 780M이 실제로 가장 흔히 쓰이는 DDR5-5600 듀얼채널 실측치(약 80-85GB/s)를 대표값으로 사용(같은 출처가 언급한 LPDDR5X-7500은 더 빠르지만 실제로는 드묾). 국내 판매가는 780M이 단품이 아니라 다양한 노트북/미니PC에 내장되는 칩이라 이번엔 채우지 못해 비워둠 — 추후 대표 기종을 확인해 채울 예정

- MLX(mlx-lm, Apple Silicon 전용)를 4번째 실행 방식으로 추가하고, llama.cpp 백엔드 기반 GUI 앱(LM Studio·koboldcpp·text-generation-webui) 호환 안내를 실행 명령어 화면에 추가. MLX 속도 배율은 자체 벤치마크가 아니라 local-llm.net의 공개 비교표(M4 Max 64GB·M3 Pro 36GB, llama.cpp Q4_K_M vs MLX 4bit, 2026-08 확인)를 근거로, 실측 구간(3-8B 모델 +3-8%p, 12-14B 모델 +14-25%p)보다 보수적으로 반올림한 계단식 배율(3-8B +5%, 9-16B +15%, 17B+는 +12%로 재수렴)을 적용 — 첫 토큰 지연(TTFT) 배율은 해당 출처에 데이터가 없어 조작하지 않고 llama.cpp와 동일(중립)로 둠. Apple GPU(M2/M3/M4 Max/Ultra 5종)를 선택했을 때만 실행 방식 선택지·비교 카드·`mlx_lm.generate` 명령어가 나타나도록 게이팅하고, `data/gpus.js`의 해당 5개 항목 `runtimes` 필드에 "MLX"를 추가해 이미 다른 Apple GPU 정규화 로직이 쓰던 것과 동일한 표기로 통일
- 모델 카탈로그에 Qwen3.8 27B(Alibaba, 2026-08-14 공개, Apache 2.0)를 새로 추가 — Gated DeltaNet 선형 어텐션과 게이트형 풀 어텐션을 3:1로 섞은 하이브리드 구조, 262K 토큰 네이티브 컨텍스트, 기본 활성화된 사고 모드가 특징. 사용자 요청으로 최신 LLM 모델 업데이트가 있는지 조사한 결과, DeepSeek V4-Pro/V4-Flash·Qwen3.6 계열·GLM-5.2 등 기존 카탈로그 항목은 이미 실제 공개 스펙(파라미터/컨텍스트/라이선스)과 일치함을 웹 검색으로 재확인해 그대로 유지. Qwen3.8-2.4T-A95B(Qwen3.8 Max의 오픈 MoE 변형)는 출시일 정보가 출처마다 8/2와 8/12로 엇갈리고 커스텀 라이선스 조건이 불명확해 이번엔 보류, "GLM-5.2 Turbo"로 불리는 변형도 기존 GLM-5.2와 구분되는 확정 스펙을 찾지 못해 추가하지 않음
- 실측 벤치마크 3건 추가(GPT-OSS 20B, Qwen2.5 72B Instruct, Llama 3.2 90B Vision — 전부 NVIDIA DGX Spark, ProX PC 실측 리뷰 인용). 데스크톱 RTX 5050은 공개된 실측치가 아직 없어(노트북 변형 수치만 존재, 다른 제품이라 미사용) 비워둠. 실측 벤치마크 카운트 10 -> 13
- DGX Spark·Apple Silicon 6종의 `gpuUsableMemoryGb`(통합 메모리 중 GPU가 실제 쓸 수 있는 양)를 지난 커밋에서 전부 75% 근사치로 채웠던 것을 플랫폼별 실제 공식 근거로 재확인 — Apple Silicon(Metal recommendedMaxWorkingSetSize)과 AMD Ryzen AI Max+ 395(Variable Graphics Memory/GTT 상한)는 75%가 실제로 맞아 그대로 유지, NVIDIA DGX Spark는 데스크톱 오버헤드가 적은 단일 목적 어플라이언스라 커뮤니티 안전 상한인 85%(96GB → 109GB)로 상향 수정. 근거를 데이터 파일 주석으로 명시
- app.js(5,184줄)에서 Hugging Face 공개 모델 직접 불러오기 관련 함수 12개(259줄)를 완전히 독립적인 블록으로 확인해 `features/hf-import.js`로 분리(estimation-engine.js 분리 전례와 동일 방식, 동작 변화 없음). app.js는 350KB 예산의 64%(225KB)로 여유 확보
- 국내 GPU 시세를 63종 → 67종으로 확대 — GeForce RTX 5050 8GB, NVIDIA DGX Spark(GB10 128GB), AMD Ryzen AI Max+ 395 64GB/128GB(미니PC 완제품가) 4종을 다나와 최저가 기준으로 새로 채움
- DGX Spark와 Apple Silicon(M3 Ultra/M2 Ultra/M4 Max/M3 Max/M2 Max) 6종이 이름은 "통합메모리"이면서도 `formFactor`/`memoryType`/`gpuUsableMemoryGb` 필드가 비어 있던 것을 발견해 수정 — 이 상태로는 GPU 어드바이저의 "통합 메모리" 필터가 이 6종을 전혀 찾지 못했고, 다중 GPU 견적 계산도 완제품 1대 가격을 gpuCount만큼 곱하는 오류가 날 수 있었음(예: DGX Spark 4대 선택 시 실제로는 존재하지 않는 "4대 견적"을 정상처럼 보여줌). 6종 모두에 `formFactor: "integrated"`를 채우고 다중 GPU 곱셈 가드(`platform-v3.js` 3곳)를 노트북 전용 조건에서 통합 메모리 시스템까지 확장. Apple Silicon 가격 자체는 이번에도 채우지 못함 — Apple 공식 스토어의 Mac Studio 메모리 선택 UI가 칩을 바꿔도 갱신되지 않는 실제 사이트 버그를 스크린샷으로 재확인했고 다나와에도 정상 매물이 없어 정직하게 비워둠
- data-health 링크 감사(issue #3)에서 실제로 깨진 GPU 출처 URL 6건(Tesla P40/P4 공식 페이지 → 실제 PDF 데이터시트 경로, RX 9060 XT 16GB/8GB·RX 9070 XT 16GB의 URL 오타, Tesla V100 페이지 → PDF 데이터시트 경로)을 3라운드에 걸쳐 fix→재실행→검증으로 확인·수정. `isOfficialDomain` 판정 로직도 nvidia.com 같은 최상위 도메인만 인정하던 것에서 `images.nvidia.com` 같은 하위 도메인까지 "공식"으로 인정하도록 넓힘(1st-party 콘텐츠인지가 기준이지 어느 서브도메인이 서빙하는지는 무관). 이전 세션이 잘못 지목했던 원인(다나와 URL)은 이미 제외 처리돼 있었고 실제 원인은 개별 죽은 링크였음을 재확인하고 커밋 메시지에 명시적으로 정정. 아울러 intel.com/openai.com/huggingface.co/gnu.org/amd.com 5개 도메인에서 반복적으로 다른 페이지가 매번 봇 차단(false positive)에 걸리는 것을 수동 접속으로 확인하고 lychee 검사 대상에서 제외. 5번째 수동 실행에서 워크플로가 완전히 초록불로 통과하고 issue #3이 자동으로 닫히는 것까지 확인
- 국내 GPU 시세를 46종 → 63종으로 확대 — RX 9060 XT 8GB/16GB, RX 9070/9070 GRE, Intel Arc B570/B580 6종, RTX PRO 5000 Blackwell 48GB/72GB·RTX PRO 4500/4000/4000 SFF/2000 Blackwell·RTX A4500·RTX 4500 Ada·RTX 4000 SFF Ada 9종, Radeon PRO W6800·Radeon AI PRO R9700 2종을 다나와 최저가 기준으로 새로 채움. Radeon PRO W7700은 다나와 매물이 2건뿐이고 하나는 VRAM 표기 오류(6GB, 실제 16GB)와 번역체 제목, 나머지는 [해외] 태그와 번역체 제목이라 둘 다 신뢰할 수 없어 추측 없이 그대로 비워둠. RTX 4090·RTX 3080 12GB·RTX 3080 Ti·RTX 3090 Ti도 재확인했으나 여전히 액세서리·중고·해외 번들·근거 없는 단일 이상치 매물뿐이라 정직한 신품가를 구하지 못해 비워둔 상태 유지
- `data/gpus.js`의 `w7800-48` 항목이 실존하지 않는 유령 GPU였음을 발견해 삭제 — vram/ram/bandwidth(48/128/864)가 `w7900-48` 행과 완전히 동일한 값으로, 복붙 후 id/name만 바꾼 데이터 오류였음. AMD 공식 W7800 스펙 페이지로 직접 확인한 결과 W7800은 32GB 단일 구성만 존재. 가격을 채우지 못한 "미확인" 항목이 아니라 애초에 잘못된 레코드였던 사례
- GPU 출처 신뢰도 스키마를 4단계(공식 모델별 사양 / 공식 제품군 페이지 / 3rd-party 참고 / 미확인)로 분리하고, 사람이 수동으로 표시하는 대신 `sourceUrl` 도메인 기준(nvidia.com/amd.com/intel.com/apple.com만 "공식")으로 자동 분류하도록 변경. 앞으로 3rd-party 링크가 실수로 "공식"이라고 잘못 표시될 수 없음. 이 과정에서 실제로 잘못 분류돼 있던 기존 데이터 일부도 함께 바로잡았고, 인기 GPU 30여 종과 나머지 GPU 전체에 대해 제조사 공식(불가능한 경우 TechPowerUp 등 신뢰할 수 있는 3rd-party) 스펙 링크를 새로 붙임
- 데이터센터·가속기 GPU 32종(NVIDIA H100/H200/B100/B200/A100/A40/A30/A10/L40/L40S/L4/T4/V100/P100/Tesla 계열, AMD Instinct MI210~MI355X, Intel Data Center Max/Flex)에 `enterpriseOnly` 플래그를 추가. 다나와 등 국내 채널을 실제로 확인해보니 이 카드들은 일반 소매가 아니라 수입·호스팅업체마다 가격이 제각각(A100 한 모델이 3천만~9천5백만원까지 벌어짐)이라, "아직 못 채운 가격"이 아니라 "일반 소매 채널이 없는 GPU"로 명확히 구분. 가격 커버리지 % 계산도 이 32종을 뺀 실제 구매 가능한 GPU 기준으로 다시 계산해, 앞으로 채워야 할 대상이 무엇인지 헷갈리지 않게 함
- 국내 GPU 시세를 15종 → 46종으로 확대 — RTX 40/30시리즈 데스크톱, Radeon RX 6000시리즈, 노트북 GPU 8종(완제품가·GPU 단품 아님을 명시하고 다중 GPU 견적에서 대수만큼 곱해지지 않도록 formFactor로 방어), RTX A/Ada/RTX PRO Blackwell·Radeon PRO W시리즈 워크스테이션급 9종을 새로 채움. 반대로 RTX 4090·RTX 3080 12GB·RTX 3080 Ti·RTX 3090 Ti처럼 실제 매물이 액세서리·중고·채굴용·해외 번들뿐이라 정직한 신품가를 구할 수 없는 카드는 추측 없이 그대로 비워둠
- 데이터 감사 스크립트(`scripts/data-audit.mjs`)에 가격 데이터 노후화 자동 경고(180일 초과 시 대상 GPU를 짚어서 경고)를 추가해, 손으로 채운 시세가 다음 확인 없이 방치돼도 아무도 모르는 일이 없도록 함
- "구매 결정 스튜디오" 추천 카드에서 가격이 실제 국내 시세인지 계산 추정치인지 구분 표시가 빠져 있던 부분(가격 비교표에는 이미 있었지만 추천 카드에는 없었음)을 채우고, 신품·중고·출시가 참고 각 경우에 맞는 문구가 실제로 화면에 뜨는지 확인하는 회귀 테스트를 추가
- 견적 상태 전체를 URL 쿼리 파라미터에 그대로 직렬화해 상세 견적 URL이 2000자를 넘기던 문제를, 기본값과 다른 항목만 저장하는 델타 인코딩으로 바꿔 해결(실측 2253자 → 253자, 약 9배 감소). 카카오톡·이메일로 공유할 때 잘릴 위험을 줄임
- data-health 워크플로가 문제를 발견하면 이슈를 열기만 하고, 다음 실행에서 문제가 해결돼도 자동으로 닫지는 않던 것을 고쳐 정상 회복 시 코멘트를 남기고 이슈를 자동으로 닫도록 함
- `tests/smoke.test.mjs`의 가격 커버리지 검증이 `KOREAN_GPU_MARKET` 행 수가 늘 때마다 하드코딩된 숫자를 손으로 맞춰야 했던 것을, 실제 데이터에서 값을 파생하고 최소 기준(하한선)만 검증하는 방식으로 바꿔 앞으로 가격을 더 채워도 테스트를 따로 고칠 필요가 없게 함
- LLM 모델 메타데이터에서 출처(`sourceUrl`)가 빠져 데이터 감사에서 "메타데이터/카탈로그 수준 출처"로 경고되던 6종(Llama 2 7B/13B/70B Chat, 초기 Llama 3 8B/70B Instruct, Vicuna 13B v1.5)에 실제로 확인한 Hugging Face 모델 카드 링크와 출시일을 추가
- 코드 구조 정리: 6,700줄이 넘던 `app.js`에서 순수 계산 로직(VRAM/속도/품질 추정 함수군, `getHardware`~`formatPercent`, 약 1,550줄)을 `features/estimation-engine.js`로 분리. 번들러 없이 전역 스크립트로 로드하는 기존 방식(`features/*.js`)을 그대로 따랐고, 함수 호출 관계는 바뀐 게 없어 화면 동작에는 차이가 없음. `app.js`는 350KB 예산의 67%(236KB)로 줄어 여유 확보
- GPU·모델·부품 데이터셋 대폭 확장 — 개인 사용자가 여전히 쓰는 구형·저가형 하드웨어를 포함하도록 GPU 29종(GeForce GTX 10/16세대 10종, RTX 20/30세대 저가형 6종, Radeon RX 6000/5000세대·RX 580 11종, NVIDIA Tesla P40/P4/M40 3종)을 추가해 총 148종으로 확대. LLM 모델은 Llama 2 7B/13B/70B Chat, 초기 Llama 3 8B/70B Instruct, Vicuna 13B v1.5 등 여전히 널리 쓰이는 구형 모델 6종을 추가해 총 160종으로 확대. 시스템 부품 카탈로그에도 구형 AM4 플랫폼(Ryzen 5 5600 + B550 보드)과 LGA1700 저가형 CPU(i5-12400)를 추가
- "BOM 호환성·용량 검증"에서 "호환 부품으로 자동 수정"을 눌러도 "파워 15% 여유" 항목이 계속 주황색(경고)으로 남아있던 문제 수정 — 자동 수정 로직이 파워서플라이를 고를 때 실제 소비전력만 보고 골랐을 뿐, 검증 규칙이 요구하는 15% 여유분은 반영하지 않고 있었음(UPS 20% 여유는 이미 반영돼 있었음). 파워서플라이 선택·수량 계산 모두 15% 여유분을 기준으로 하도록 수정. 아울러 CPU·파워서플라이·UPS·메모리·스토리지·NIC·서버 케이스 카탈로그에 상·하위 등급을 여러 종 추가(예: 파워서플라이 550W~5000W, UPS 1~20kVA)해 선택 폭을 넓힘
- 가격·근거 커버리지 안내문(`.price-coverage-note`/`.evidence-coverage-note`)과 근거 출처 배지(`.evidence-status`)도 다크 모드에서 배경이 거의 안 보이거나 색이 안 바뀌던 같은 종류의 미정의 변수(`--soft`) 문제였음을 추가로 확인해 수정. 배지는 등급 pill과 같은 초록/노랑 톤 변수로 통일. 참고로 "데이터 완성도" 텍스트 색은 테마 변수로 바꿨다가 CI 접근성 검사(색 대비)에 걸려 원래의 고정 색으로 되돌림 — 배경이 없는 일반 텍스트라 다크 모드용 값까지 따로 검증하지 않고는 바꾸면 안 되는 자리였음
- CI가 4개 커밋(체크박스 마크업 통일부터 방금 전 여백 수정까지) 연속으로 조용히 실패하고 있던 문제 수정 — 원인은 UI 코드가 아니라 낡은 테스트였음. 체크박스 마크업을 통일하며 이제 쓰이지 않는 `.studio-check-label` CSS를 지웠는데, smoke 테스트 하나가 여전히 그 옛 규칙이 남아 있는지를 확인하고 있어서 계속 실패하고 있었음. 실제 화면은 매번 정상적으로 배포되고 있었지만(라이브 확인은 매번 했음), CI 초록불까지는 확인하지 못한 채 다음 수정으로 넘어간 것이 원인 — 테스트를 새 마크업 기준으로 갱신
- 다크 모드에서 주요 패널(핵심 작업 전환 탭, 하드웨어·결과·GPU 어드바이저·벤치마크 패널 등) 테두리가 다크 배경에 안 어울리는 밝은 회색으로 떠 보이던 문제 수정 — 테두리 색을 정하는 CSS 변수가 실제로는 존재하지 않는 `--border`를 참조하고 있어 테마와 무관하게 항상 라이트 모드 고정값으로 떨어졌던 것이 원인. "데이터 완성도" 배지 색상도 같은 이유로 테마에 안 맞게 고정돼 있던 것을 함께 수정
- "고객·워크로드 상세 입력"·"인프라·운영 조건 상세" 카드 안에서 첫 줄 필드("제안 회사", "PCIe" 등)가 카드 위쪽 테두리에 바짝 붙어 있던 문제 수정 — 위쪽 여백만 0으로 빠져 있거나 아예 안쪽 여백이 없던 두 영역에 다른 변과 동일한 16px 여백을 추가
- "개발계·운영계 분리" 체크박스의 라벨 텍스트가 세로로 한 글자씩 쪼개져 표시되던 문제 수정 — 우선 이 체크박스만 다른 체크박스들과 다르게 체크박스·글자를 감싸는 span을 하나 더 두는 마크업이었던 것을 다른 체크박스들과 동일한 구조로 통일. 그런데도 문제가 남아 다시 확인해보니, 이 체크박스는 "고객·워크로드 상세 입력" 폼의 필드 도움말(물음표 버튼·"시작 기준" 안내문)이 자동으로 함께 붙는 유일한 체크박스였고, 그 안내문에는 항상 한 줄 전체(100%)를 차지하도록 하는 규칙이 있었음. 앞서 다른 체크박스들의 줄바꿈 문제를 고치며 이 영역을 "줄바꿈 금지"로 바꿔둔 탓에, 안내문이 새 줄로 내려가지 못하고 체크박스·라벨과 한 줄에서 억지로 공간을 나눠 가지면서 라벨 텍스트가 몇 글자 폭으로 짓눌려 세로로 쌓였던 것이 진짜 원인. 이 카드 형태 체크박스에 한해서만 줄바꿈을 다시 허용해 안내문은 아래 줄로, 체크박스·라벨은 항상 한 줄로 유지되도록 수정
- 물음표(?) 도움말 버튼이 화면 폭과 무관하게 계속 세로로 길쭉한 타원으로 늘어나 보이고, 그 옆 체크박스도 어긋나 보이던 문제 재수정 — 앞서 모바일 폭(680px 이하) 전용 규칙에서는 이 버튼을 제외했지만, 실은 모든 버튼·입력창에 최소 높이(44px)를 강제하는 별도의 전역 디자인 규칙(components.css, 화면 폭 조건 없음)이 따로 있었고 거기에는 제외 처리가 안 돼 있었음. 이 전역 규칙과, 같은 파일의 520px 이하 모바일 규칙 두 곳 모두에서 물음표 버튼을 제외해 항상 18px 원형으로 고정
- 기업 컨설팅 모드의 체크박스("서비스·스토리지·관리망 분리", "스트리밍 응답", "자동 확장" 등)가 크기·라벨 연결 스타일 규칙이 없어 브라우저 기본 크기로 크게 렌더링되고 좁은 칸에서 체크박스와 라벨 텍스트가 서로 다른 줄로 떨어져 무엇을 켜는 체크박스인지 알아보기 어렵던 문제 수정 — 크기를 18px로 통일하고 체크박스·라벨이 항상 한 줄에서 시작하도록 정렬
- "모델로 찾기(GPU 어드바이저)" 화면에서 "이 GPU 선택" 버튼을 눌러도 아무 반응이 없던 문제 수정 — 클릭 시 내부적으로는 GPU가 선택됐지만 화면이 여전히 "모델로 찾기" 모드에 머물러 있어 결과가 표시될 하드웨어·결과 패널 자체가 숨김 처리돼 있었음. 이제 선택 즉시 "GPU가 이미 있어요(찾기)" 화면으로 전환해 해당 GPU로 실행 가능한 모델을 바로 보여줌
- 견적 화면 전반의 카드·버튼 테두리와 배경이 일부 사라져 보이던 문제 수정 — CSS 변수 이름이 실제 정의된 색상 토큰과 어긋나 있어(`--border`, `--surface`, `--accent`, `--text` 등 미정의 변수) 해당 규칙이 적용되지 않고 있었음. "간편 견적" 서비스 유형 선택 카드, 기업 컨설팅 모드의 V3.8~V4.8 블록(SLA, 이력, 제안서, 랙 구성, 승인 흐름, 3안 비교 등) 약 50곳의 테두리·배경·강조색을 실제 디자인 토큰으로 연결
- 상세 설정 라벨 옆 물음표(?) 도움말 버튼이 텍스트 기준선과 어긋나 붕 떠 보이던 문제 수정 — 세로 정렬을 맞추고 크기를 22px에서 18px로 줄여 라벨 텍스트와 자연스럽게 어울리도록 조정
- "처음 사용 가이드" 팝업의 4가지 경로 카드를 크기가 제각각인 2x2 그리드에서 행(row) 단위 목록으로 변경해 높이가 들쭉날쭉하던 문제 수정. 또한 경로 선택 시 상단 탭만 잠깐 반짝이고 화면 전환만 되던 것에서, 실제로 입력해야 할 필드(GPU 검색·모델 검색·서비스 유형 등)까지 스크롤해 하이라이트·포커스하도록 변경 — 입력 항목이 화면 여기저기 흩어져 있어 어디부터 시작해야 할지 헷갈리던 문제 개선
- 영어 모드에서 "테마 선택"·"현재 작업 진행 단계"·"가이드 닫기"·"60초 샘플 체험"·배치 플래너/모델 검색 aria-label 등 일부 접근성 라벨과 하단 "로컬 사용 요약" 패널이 여전히 한국어로 남아있던 문제 수정 — 페이지 로드 시 캡처된 원본 한국어 문구를 매번 다시 번역하는 방식이라, "선택"→"selected"·"배치"→"batch" 같은 짧은 단어 단위 치환 규칙이 더 긴 문구 안에 끼어 있는 단어만 부분적으로 바꿔 놓아 "테마 selected"처럼 한/영이 섞이던 것이 원인. 해당 문구들을 통짜 문장으로 사전에 추가해 부분 치환보다 먼저 매칭되도록 수정
- "간편 견적" 마법사의 ①②③④ 단계 표시가 보기 전용이었던 문제 수정 — 클릭·키보드(Enter/Space)로 원하는 단계로 바로 이동 가능
- "GPU가 이미 있어요"·"직접 GPU 추가"·모델 확인(GPU 어드바이저) 화면에서 필터·GPU 인벤토리를 조작한 뒤 영어 모드로 다시 렌더링될 때 등급 pill·표 헤더·워크로드 라벨 등이 한국어로 되돌아오던 문제 수정 — 해당 3개 화면도 전체 번역 사전 스윕을 적용하도록 변경
- 인프라 견적 계산에서 실제로는 도달하지 않는 폴백(fallback) 코드 경로를 정리해 platform-v3.js 용량 여유 확보 (동작 변화 없음)
- H100·H200·A100·L40S·RTX PRO 6000 Blackwell 등 인프라 견적 기본 GPU를 포함해 GPU 8종에 제조사 공식 모델별 스펙 페이지 링크 추가
- 최근 공개된 LLM 4종(Kimi K2.7 Code, Kimi K3, NVIDIA Nemotron 3 Super 120B A12B, Poolside Laguna XS 2.1)과 GPU 3종(NVIDIA B300 Blackwell Ultra, AMD Instinct MI350X·MI355X)을 공식 출처·발표 벤치마크와 함께 추가
- 간편 견적 결과에서 예산에 맞는 구성이 없어 최저비용안을 대신 보여줄 때, "더 가벼운 모델로 다시 계산" 버튼을 추가해 한 번의 클릭으로 비용 우선(경제형) 모델로 재계산할 수 있게 함
- "처음 사용 가이드" 팝업을 화면 안에 끼워 넣는 방식에서 배경을 어둡게 하고 화면 중앙에 띄우는 모달 팝업으로 전환 — 다른 콘텐츠와 뒤섞여 잘 눈에 띄지 않던 문제 수정. 데스크톱은 중앙 카드, 모바일(≤760px)은 화면 하단에서 올라오는 바텀시트로 표시하고, 배경 클릭·Esc 키로 닫을 수 있게 함
- "GPU가 이미 있어요" 모드에서 GPU 선택 뒤 "현재 단계 ① GPU 선택 → ② 추천 3개 확인 → ③ 전체 모델 탐색" 진행 표시가 "전체 모델 탐색" 탭으로 전환해도 계속 ②에 멈춰 있던 문제 수정. 진행 표시를 갱신하는 코드가 GPU 선택 여부만 확인하고 간편/전체 모드 전환은 반영하지 않고 있었음 — 전체 모델 탐색 모드일 때 ③번을 현재 단계로 표시하도록 수정
- v7.6 실측 제보·벤치마크 데이터 확인을 4번째 작업 모드로 분리하고, 처음 사용 가이드 팝업을 상황 묘사형 문구·소요 시간 표시·클릭 연결·질문형 마법사로 개편했으며, 가격 숫자 줄바꿈·의사결정 허브 오노출·패널 폭 불일치·전문가 모드 겹침·모바일 가로 스크롤·영어 모드 혼용 텍스트 등 UI 버그 다수를 수정
- v7.5 실행 명령 옆 실측 제보, 터미널·JSON 파싱, 개인정보 제거 미리보기, 동일 조건 비교, 공개 기여자와 우선 측정 조합 10개를 추가
- v7.4 Lighthouse 성능 예산, axe 접근성 검사, 6개 화면 폭·키보드·긴 이름·빈 결과·인쇄 PDF 회귀 검사를 CI에 통합
- v7.3 고객명·직접 입력·견적 금액을 수집하지 않고 정해진 행동 횟수만 브라우저에 보관하는 로컬 사용 흐름 요약을 추가
- v7.2 우선 GPU 30개 공식 제조사 링크, 30·90일 가격 신선도, GPU·모델 데이터 완성도와 주간 공식 출처 검사를 추가
- v7.1 핵심 번역 키 카탈로그와 GPU Advisor·모델 배치·벤치마크 모듈 분리, 공통 UI CSS, 벤치마크 지연 로딩 및 app.js 350KB 예산을 적용

- v7.0 첫 화면에 60초 무입력 체험과 작업 흐름 피드백 양식을 추가하고, 구성 선택 뒤 피해야 할 조건·PoC 다음 행동·절감 순서를 설명하며 GitHub Actions를 Node 24 기반 최신 메이저로 갱신
- v6.9 계산 테스트를 5개 병렬 CI 작업으로 분리하고 정적 파일 크기 예산을 검사하며, CI 성공 뒤 Pages가 전체 검증을 반복하지 않도록 개선
- v6.8 경제형·권장형·확장형 카드를 6개 핵심 수치와 한 문장 이유로 축소하고 QPS·배치·예비 GPU·운영 조건은 선택 상세로 이동
- v6.7 국내 GPU 가격 행의 출처·날짜·금액·중복을 자동 검증하고 30일·90일 기준 신선도와 누락 수를 표시
- v6.6.1 신뢰도와 중고가 계산 방식의 잔여 한영 혼용을 제거하고 KRW·USD 표시 기준을 공통 로케일 모듈로 정리
- v6.6 단계 안내·검색·출처 정책·전용 스타일을 독립 모듈로 분리하고 정적 빌드와 smoke test에 포함하며 저장·공유·내보내기 버튼의 선택자 연결 오류 수정
- v6.5 GPU·모델·국내 가격 Issue Form에 중복 확인을 추가하고 처리 중·완료 요청 조회 링크와 상태 흐름 제공
- v6.4 GPU·모델 검색에 오타 허용, 별칭, 한국어 용도, 메모리·폼팩터 자연어 조건 검색과 추천 후보 제공
- v6.3 모델별 공식·제품군·외부·누락 출처를 분리하고 인기 GPU 우선 검증, 가격 출처 범위와 보강 링크 표시
- v6.2 인프라 결과를 가격·GPU·동시 사용자·VRAM·전력·근거 중심 3안과 다음 행동으로 단순화
- v6.1 작업 시작 후 첫 선택 화면을 접고 간편 견적을 서비스→사용자→우선순위→결과 단일 단계 화면으로 전환
- v6.0 첫 화면을 `GPU 보유·실행 모델 보유·AI 서비스 구축` 세 가지 상황으로 재설계하고 30초 가이드·현재 단계·입력 없는 샘플·모바일 흐름을 추가
- v5.9 GPU·모델·국내 가격 요청 센터와 요청 처리 단계, 이름·별칭 중복 확인 및 미리 채운 GitHub 제보 링크 추가
- v5.8 챗봇·RAG·문서 VLM·폐쇄망 비서·이미지·영상·음성·아바타를 설명형 서비스 카드로 바꾸고 간편 견적 진행 단계를 표시
- v5.7 고객명·담당자·공급사 내부 필드를 제외한 읽기 전용 고객 결과 화면과 인쇄·PDF 저장 흐름 추가
- v5.6 날짜와 출처가 있는 국내 시세 범위를 표시하고, 미확인 가격을 공급사 견적·직접 입력 대상으로 명확히 구분
- v5.5 선택 구성의 추천·제외 이유와 동시 요청·답변 길이·성장 여유를 조정하는 비용 절감 시뮬레이션 추가
- v5.4 영문 UI 잔여 문구·영문 Excel 산출물을 정리하고 GPU·모델·워크로드 정적 상세 페이지를 한국어/영어 경로와 hreflang으로 동시 생성
- v5.3 한국어 GPU Advisor의 기본 통화를 원화로 전환하고 국내 시세·출시가 참고·공급사 견적 필요·계획 가정값을 구분하는 가격 정책 모듈 추가
- v5.2 QPS·동시 요청·배치 효율·안전 이용률을 분리하고 운영·예비·개발 GPU와 장애 처리량·대기열 상태를 계산하는 인프라 산정 엔진 추가
- v5.1 GPU 찾기·모델로 GPU 찾기·여러 모델 배치·인프라 견적을 독립 작업공간으로 분리하고 비활성 화면 렌더링과 의사결정 도구 초기 로딩 축소
- v5.0.1 모든 STT/TTS·이미지·비디오 모델에 같은 용도를 일괄 부여하던 규칙을 제거하고 모델 유형·메타데이터·태그·계산 추정 근거를 용도별로 기록
- v5.0 추천 용도 엔진과 실행 결과 제보를 독립 모듈로 분리하고 310개 모델의 용도·언어·입출력·품질·지연 특성을 공통 스키마로 정규화
- v4.9.5 실제 Chromium에서 1920·1440·1280·768·430·390px 화면, 워크로드 격리, 한영 전환, 정적 페이지와 가로 넘침을 검사하는 CI 추가
- v4.9.4 GPU 116개·모델 310개·워크로드 11개의 검색용 정적 상세 페이지, canonical·JSON-LD·sitemap.xml·robots.txt 자동 생성
- v4.9.3 실행 성공·실패 결과를 GPU·모델·런타임·계산 조건이 채워진 GitHub Issue로 제보하는 흐름 추가
- v4.9.2 모든 워크로드의 용도를 명시적 capability 데이터로 연결하고 추천 카드에 해당 용도를 지원하는 이유 표시
- 빠른 추천의 용도를 LLM·임베딩·리랭커·OCR/VLM·이미지·비디오·아바타·STT·TTS별로 분리하고, 선택한 종류 밖의 모델이 이전 결과로 남는 오류와 한영 전환 잔여 문구를 수정
- v4.9 견적 준비도 대시보드와 요구사항→3안 비교→산출물 바로가기를 추가하고, 필수 입력과 가격·출처 보강 항목을 분리 표시
- 사용자·동시 요청·QPS·토큰 길이·SLA 관계를 검증해 잘못된 입력에 `aria-invalid`, 안내 문구와 직접 이동 기능 제공
- 경제형·권장형·확장형에 모델 메모리·정상 처리량·장애 처리량·예산 조건 충족도와 경제형 대비 비용·처리 여유 표시
- 공유 URL을 인프라 관련 상태만 담는 스키마 3으로 축소하고 브라우저 초안 자동 복원 및 JSON 준비도 메타데이터 추가
- 탭 방향키 탐색, 모바일 도움말 경계 계산, 전체 행 18px 체크박스와 반응형 준비도·비교 UI 추가
- 한국어·영어 README에 v4.8.2부터 v4.9까지 한 줄 버전 이력을 추가하고 패키지 버전을 4.9.0으로 갱신
- v4.8.1에서 변경된 `Find a GPU for my model` 버튼명을 전체 회귀 테스트에도 반영해 CI 실패 수정
- 인프라 설정의 명칭을 `상세 설정`으로 통일하고, 고객·워크로드·SLA·운영 조건의 모든 입력에 일반 사용자용 용어 설명과 시작 기준을 추가
- 소규모 체험·팀 운영·운영 서비스 기준값을 한 번에 적용하는 3개 베이스라인 프리셋 추가
- v4.8.1 사전 홍보 UX 정리: 첫 화면을 GPU→모델, 모델→GPU, AI 인프라 견적 3개 진입점으로 단순화하고 여러 모델 배치를 고급 도구로 이동
- RTX 3060 빠른 추천과 사내 RAG 30명 샘플을 추가하고, 인프라 간편 견적을 서비스·사용자 수·예산/품질의 3단계 입력으로 통일
- 경제형·권장형·확장형 카드 첫 화면을 총 예상 가격, GPU, 동시 사용자, VRAM 여유, 소비전력, 신뢰도, 추천 이유 중심으로 축소하고 상세 인프라는 선택 시 표시
- 공식 사양·공식 모델 정보·외부 공개 참고값·계산 추정·사용자 측정·검증 필요 상태와 예상 속도 범위·오차 이유를 통일
- 국내 시세·출시가 참고·공급사 견적 필요를 구분하고, 출처와 기준일이 있는 국내 가격만 성능/가격 순위에 사용
- 본문 바로가기, 44px 터치 영역, 모달 포커스 복귀, ESC 닫기, 빈 결과 필터 초기화, 저장·복사 알림과 모바일 헤더·첫 화면 간격 보정
- 인프라 기능 지연 로딩, 공통 UI 기반 모듈, 48초 내 8개 핵심 흐름 smoke test와 6개 고정 뷰포트 QA 절차 추가
- 모델로 GPU 찾기가 이전 GPU의 긴 컨텍스트를 물려받아 모든 GPU를 부적합으로 표시하던 문제와 인프라 탭이 상담 화면으로 되돌아가던 문제 수정
- CI의 jsdom 회귀 테스트를 세 개의 독립 프로세스로 분할해 Node heap 메모리 누적으로 전체 작업이 실패하던 문제 수정
- 상세 인프라 견적에 CPU·RAM·스토리지·NIC·파워·UPS·서버 케이스 선택, 수량·추가 비용·GPU 포함 합계와 Excel BOM 내보내기 추가
- Excel·편집 템플릿·PDF 등 산출물 버튼의 높이와 글자 수직 정렬 통일
- v4.8 정식 릴리스 노트, 실제 화면 데모 GIF, 1280×640 소셜 미리보기와 한국어·영어 홍보 문안 추가
- RAG·챗봇·AI 아바타 대표 사례를 사용자 수와 함께 바로 여는 공유 URL 및 README 진입 링크 추가
- 검색·소셜 공유용 설명, Open Graph 메타데이터와 저장소 About·Topics 권장값을 현재 인프라 산정 포지셔닝에 맞게 갱신

### v4.8 — 3안 통합 의사결정

- 경제형·권장형·확장형을 최종 제안가, GPU·CPU·RAM, SLA, 랙·전력·냉각, 장애 처리량, BOM 검증과 근거 신뢰도로 비교
- 각 대안의 선택·제외 이유를 한 표에서 확인하도록 구성

### v4.7 — 견적 검토·승인

- 초안·검토 중·승인·수정 필요 상태와 담당자·검토자·승인자·승인 시각 추가
- 저장 프로젝트 JSON과 버전 이력에 승인 상태와 최종 제안가 포함

### v4.6 — 랙·네트워크·전원

- 랙 U·스위치·NIC 링크·광모듈·케이블·A/B PDU 회로·PUE 기반 냉각 요구량과 기반시설 비용 산정
- 서비스·스토리지·관리망 분리 옵션과 자동 토폴로지 표시

### v4.5 — 상업 견적 가격

- 공급사·견적번호·가격 유형·확인일·유효기간·환율 입력 추가
- 카탈로그·기반시설 금액에 할인·마진·부가세를 순차 적용한 최종 제안가 계산

### v4.4 — BOM 검증

- CPU·메인보드 소켓, CPU 코어, RAM·스토리지, NIC, PSU·UPS 여유, GPU 서버 장착 검증
- 호환되지 않는 구성을 자동 추천 부품으로 복구하는 기능 추가

### v4.3 — 초보자용 독립 인프라 견적

- 기본 GPU 검색 화면에서 AI 인프라 견적을 별도 작업 화면으로 분리
- 용도·품질·사용자 규모·구축 위치만 고르면 모델과 GPU·CPU·RAM·NVMe·NIC·전원을 자동 산정하는 간편 견적 추가
- 기존 SLA·BOM·TCO 입력은 상세 견적으로 분리하고 기본 화면에서는 접힌 상태로 변경
- Threadripper·Xeon W·EPYC, 64GB~2TB ECC RAM, Enterprise NVMe, 10~400GbE, 4/8-GPU 서버, 이중화 PSU와 UPS 카탈로그 확장
- 챗봇·이미지·영상·음성 상담·AI 아바타 용도, 사용자 수 직접 입력, 클릭형 3안 상세 비교와 GPU·클라우드 가격 근거 링크 추가
- 빠른 찾기의 모델 결과를 워크로드 탭 바로 아래로 이동하고 GPU Advisor를 `예산·전력 GPU 추천` 독립 작업으로 분리
- AMD Ryzen AI Max+ 395 / Radeon 8060S 64GB 통합메모리 구성 추가

### v4.2 — 온프레미스·클라우드 비교

- 서버 구매비, 전력, 상면, 유지보수, 지원, 클라우드 시간당 비용을 반영한 1·3·5년 TCO와 손익분기점 추가

### v4.1 — 벤치마크 실행 도우미

- vLLM·llama.cpp·Ollama·NIM 명령 생성, 성능/부하 단계, 결과 JSON, 보정계수·이상치·PoC 판정 추가

### v4.0 — 제안서 산출물

- 고객 요약본·기술 검토본, 자동 구성도·배치도, 편집 가능한 Excel과 인쇄용 PDF 예제 추가

### v3.9 — 프로젝트·견적 이력

- 브라우저 로컬 프로젝트 목록, 견적 버전·변경 비교·복제, JSON 내보내기·가져오기 추가

### v3.8 — 실시간 SLA

- RPS와 동시성 분리, TTFT·ITL·전체 p95, 큐·배치·복제본·자동 확장과 아바타 단계별 지연 계산 추가

### v3.7 — 아바타·립싱크 모델

- MuseTalk 1.5, LivePortrait, SadTalker, Wav2Lip을 공식 출처·라이선스 경고와 함께 전용 모델 카탈로그에 추가
- 전체 모델 탐색, GPU Advisor, AI 스택 배치에 `아바타·립싱크` 워크로드와 GPU VRAM·처리 속도 계산 연결
- AI 아바타 채팅 프리셋의 영상 단계를 일반 비디오 생성 모델에서 실시간 MuseTalk 립싱크 모델로 교체
- 모델별 실행 초안과 LivePortrait 의존 가중치·Wav2Lip 비상업 제한 안내 추가

### v3.6 — 멀티모달 AI 스택

- AI 스택 모델 선택에 STT와 TTS 카테고리를 추가하고 오디오 모델의 VRAM·실시간 배속을 GPU 배치 계산에 연결
- Whisper STT → LLM → TTS → 아바타 영상 생성 흐름을 한 번에 구성하는 AI 아바타 채팅 프리셋 추가
- 사용자 화면의 `SI 상담` 표현을 더 넓고 이해하기 쉬운 `AI 인프라 견적`으로 변경

### v3.5 — AI 인프라 Pre-sales 산정 완성

- v3.1.1 상세 고객·서비스·SLA 입력, v3.2 서버 토폴로지·모델 배치·장애 여유, v3.3 인프라 BOM을 통합
- v3.4 도입비·연 전력비·3년 TCO, 편집형 Excel, PDF 인쇄, 배포 초안 산출물 추가
- v3.5 모델·GPU·VRAM·처리량 근거별 신뢰도와 TTFT·속도·오류율·안정성 PoC 판정 추가

### v3.1 — SI 인프라 사전 견적 상담

- 고객의 구축 목적·모델·사용자·동시 요청·토큰·응답 목표·가용성·성장 여유를 받는 Pre-sales 상담 모드 추가
- 경제형·권장형·확장형 3안의 GPU·노드·CPU·RAM·NVMe·네트워크·설비 전력 사전 산정
- 사내 문서 RAG·문서 OCR/VLM·폐쇄망 AI 비서 프리셋과 공유 가능한 상담 URL 제공
- 제안 근거·신뢰도·PoC 체크리스트, Excel 기초표, 인쇄/PDF 산출물과 실무 예제 추가

### v3.0 — 구매 결정 화면 재설계

- GPU Advisor의 실행 모델 개수 표시를 레이블 영역에 고정하고, 제조사·형태 조건이 정확히 맞지 않아도 실행 가능한 가까운 대안과 조건 차이를 표시
- 추천 결과를 최저 비용·균형·최고 성능 3개 카드로 단순화하고 가격·속도·VRAM·전력·실행 난이도와 추천 이유를 함께 표시
- v2.3~v2.8의 가격·추천·직접 모델·부품·런타임·실측 기능을 반응형 구매 결정 스튜디오로 통합

### v2.8 — 커뮤니티 실측 강화

- 웹 폼에서 기여자·모델·GPU·런타임·정밀도·컨텍스트·속도·환경·근거 URL을 검증하고 로컬 초안 및 GitHub 이슈 생성
- 기존 동일 모델·GPU 측정 중앙값과 3배 이상 차이나는 값을 이상치 후보로 표시

### v2.7 — 런타임 호환성

- NVIDIA CUDA/vLLM, AMD ROCm/Vulkan, Intel OpenVINO/oneAPI, Apple MLX/Metal의 운영체제별 난이도와 경고 제공
- 선택한 모델·GPU 조합에 맞는 시작 명령 표시

### v2.6 — 부품 데이터베이스

- CPU·메인보드·파워·케이스 제품 선택과 소켓·폼팩터·GPU 길이·슬롯·PSU 용량·커넥터 호환 검사
- 원화 시스템 합계, 기기 저장, URL 공유와 CPU 오프로딩 병목 경고 제공

### v2.5 — 직접 입력 모델 계산기

- 모델명·Hugging Face 주소·총/활성 파라미터·레이어·정밀도·컨텍스트·VLM 여부로 VRAM 구성 계산
- 실행 가능한 GPU 후보 표시와 계산 내용을 모델 제보 이슈로 전환

### v2.4 — 질문형 맞춤 추천

- 모델 종류·실행 모델·목표 속도·원화 예산·신품/중고·형태·전력·소음·미디어 조건 입력
- 조건을 만족하는 GPU에 자연어 추천 이유와 예상 VRAM 여유 제공

### v2.3 — 국내 가격 데이터

- 출처·갱신일이 있는 주요 GPU 국내 신품 최저가와 계산용 중고 참고가 제공
- 원화 예산, 성능/가격 및 VRAM/가격 순위, 30일 이상 지난 데이터 경고 지원

### v2.2 — 사용자 빌드 계산기

- GPU Advisor의 실행 모델을 LLM·VLM·이미지·비디오·임베딩·리랭커·OCR·STT·TTS로 분류하고 이름·제공사·태그 부분검색 지원
- 모델·GPU·CPU 등급·RAM·파워·케이스 GPU 장착 길이를 함께 입력하는 사용자 빌드 계산기 추가
- 기존 모델 계산 엔진을 이용해 실행 가능·조건부 실행·구성 변경 필요 상태와 CPU 오프로딩 용량을 판정
- GPU와 CPU 전력, 시스템 여유와 순간 부하를 반영한 권장 파워 및 권장 시스템 RAM 계산
- GPU 길이와 케이스 장착 한도를 비교하고 GPU·RAM·파워·케이스·CPU의 업그레이드 우선순위와 이유 제공
- GPU부터 기타 부품까지 가격을 입력해 USD 또는 원화 기준 전체 시스템 가격 계산
- 빌드 설정을 URL에 보존하고 한국어·영어·모바일 단일 열·접근성 레이블 지원

### v2.0 — 신뢰도·상세 주소·구매 판단·실행 도우미

- 동일 GPU·모델·런타임·정밀도·컨텍스트 실측을 그룹화하고 표본 수, 중앙값, 범위, 95% 신뢰구간을 계산하는 신뢰도 시스템 추가
- 검색, 실측 전용 필터, 속도·최신·신뢰도 정렬을 지원하는 벤치마크 2.0 추가
- GPU와 모델별 공유 가능한 상세 URL 및 선택 환경 기준 VRAM·속도 요약 추가
- 신품·중고가, 현재 GPU 처분가, 원화 환산, 전기요금, 사용 기간을 반영하는 TCO 구매 Advisor 추가
- Ollama Modelfile, llama.cpp, vLLM, Docker Compose 실행 설정 생성·복사·다운로드 기능 추가
- GPU와 모델을 각각 최대 4개까지 비교하고 모바일 비교 카드를 개선
- 전체 신규 화면을 한국어·영어 번역 키로 관리하고 키 누락·스키마·중복·출처를 검사하는 데이터 감사 추가
- 키보드 레이블, 모바일 레이아웃, 모션 감소 설정을 보강하고 주간 데이터 상태 점검 워크플로 추가

### v1.5 — 카탈로그·음성·모델 중심 탐색

- Radeon RX 9070 GRE/9060 XT, Intel Arc Pro B70/B65/B60/B50, RTX PRO 5000/4500/4000/4000 SFF/2000 Blackwell을 포함해 GPU 115종으로 확대
- 모델 카드·라이선스·중복·워크로드를 검증하고 `model-ready` 라벨로 데이터 PR을 생성하는 모델 제보 자동화 추가
- GPU를 선택하지 않아도 모델·예산·전력 조건으로 추천을 시작하는 `모델로 GPU 찾기` 진입 화면 추가
- Advisor에 현재 GPU 시세 입력, 현재 GPU 대비 속도, 가격 대비 속도 지표 추가
- Whisper·Kokoro·MeloTTS·XTTS·Fish Speech 기반 STT/TTS 모델 10종과 실시간 배속 계산 엔진 추가
- 실측 행·GPU·모델 범위, 외부 참고값, 평균 추정 오차와 우선 측정 대상을 보여주는 벤치마크 대시보드 추가
- 신규 동적 화면을 키 기반 한글/영문 사전으로 생성하고 영어 회귀 검증 범위 확대
- 모바일 GPU 비교표를 GPU별 카드 UI로 전환

### v1.4 — 실측·비용·다국어 기반 추천

- 실행할 모델을 먼저 선택하고 예산, 제조사, 폼팩터, 출시가 참고값, 월 전력비로 GPU를 추천하는 GPU Advisor 추가
- GPU 상세 화면에 공식 출처 여부와 검증일을 표시하고 가격은 실시간 시세가 아닌 참고값임을 명시
- 이미지·비디오 생성 계산에 Sage/Flash Attention, TeaCache 및 결합 최적화의 메모리·속도 보정 추가
- GPU Issue 템플릿과 자동화 파서를 정상적인 한글/영문 필드 구조로 재작성
- 공식 제조사 URL, 중복, 통합 메모리 범위, 노트북 TGP를 검증하고 PR 전 변경 미리보기 표 생성
- GPU Advisor와 생성 최적화 설정을 공유 URL에 보존하고 영문 화면 한글 잔존 회귀 검사 확대
- 데스크톱·노트북·데이터센터·통합 메모리 폼팩터 필터 정규화

- 영어 모드의 GPU 활용 요약 카드에서 동적 숫자 뒤에 한글 문구가 남던 문제 수정
- GPU 활용 요약 영역의 좌우 여백을 다른 콘텐츠와 정렬
- RTX 5090/5080/5070 Ti/5070/5060 Laptop, Radeon RX 9070, Intel Arc B580/B570 추가

### GPU 플랫폼 및 생성 워크로드 고도화

- GPU 94종을 공통 스키마로 정규화하고 RTX 4090/4080/4070 Laptop GPU와 TGP 기반 성능 보정을 추가
- GPU 상세 정보와 최대 3개 GPU 비교 화면 추가
- 이미지·비디오 생성 전용 VRAM/속도 엔진에 해상도, 스텝, 프레임, FPS, LoRA, 오프로딩 반영
- 동일 GPU·모델의 사용자 측정값을 이용한 속도 보정, 표본 수와 분산 기반 신뢰도 표시
- GPU 및 벤치마크 Issue 자동 검증과 승인 라벨 기반 데이터 PR 생성 워크플로 추가
- GitHub Pages 정적 빌드에 커밋 해시 기반 캐시 버전 자동화 추가
- 모바일 터치 영역, 가로 탭 탐색, 키보드 포커스, 모션 감소 접근성 개선
- README 한국어/영어 문서를 최신 기능 기준으로 재작성하고 영문 UI 회귀 검증 추가

- 모델을 모두 선택 해제한 상태에서 배치를 다시 계산하면 이전 3개 배치안 비교 카드가 남아 있던 문제 수정
- README의 현재 모델 찾기·AI 스택 배치 화면을 새 스크린샷으로 교체하고, 두 핵심 작업과 운영 방식 선택 가이드를 한·영 문서에 보강
- AI 스택 배치 플래너 헤더에 페이지를 벗어나지 않고 4단계 흐름과 파이프라인·독립 서비스·순차 실행의 차이를 확인하는 `사용 가이드` 추가
- 여러 GPU 배치 기능을 `상세 설정 > 고급 도구`에서 분리해 앱의 두 번째 핵심 작업인 `여러 모델 함께 돌리기 > AI 스택 배치 플래너` 전용 화면으로 승격. GPU 미선택 상태에서도 `?mode=placement`로 직접 진입할 수 있고, 현재 GPU 옆 버튼·빠른 추천·모델 상세·비교 선택 바에서도 선택 상태를 유지한 채 플래너로 이동
- 배치 플래너 첫 진입에 RAG 기본 구성, 문서 AI 구성, 여러 LLM 서비스, 직접 선택 프리셋을 추가하고 1 하드웨어 → 2 모델 선택 → 3 운영 목표 → 4 배치 결과 흐름으로 재구성. 전체 290개 모델은 검색 또는 워크로드 탐색 전까지 숨기고, 최소 VRAM 여유·자동 양자화·컨텍스트 축소·복제는 `세부 조건`에 접어 초보자 진입 부담을 축소
- 배치 결과는 한 줄 결론·안전 동시 접속·병목 모델과 균형형/처리량형/주 모델형 3개 배치안을 먼저 표시하고, GPU별 가중치·KV cache·런타임·탐색 상태 수와 실행 설정 내보내기는 `계산 상세 보기` 아래로 이동. README 한·영 문서에도 플래너 직접 링크와 전용 사용법 추가
- 빠른 추천 1·2·3순위 카드를 누르면 선택 카드가 전체 폭으로 늘어나면서 다른 순위가 재배치되고 전체 상세 분석이 길게 펼쳐지던 구조를 제거. 세 카드는 항상 같은 위치에 유지하고, 데스크톱에서는 우측 컴팩트 인스펙터·태블릿에서는 우측 오버레이·모바일에서는 하단 시트에 판정·권장 설정·VRAM·속도·추천 이유·실행 명령만 먼저 표시하며 전체 분석은 별도 버튼으로 연결
- 여러 GPU 배치를 고정 8% 여유의 순차 휴리스틱에서 제약 기반 후보 탐색으로 확장. 최소 VRAM 여유율, 양자화/정밀도 자동 변경, 컨텍스트 자동 축소, 주 모델 우선, 모델별 요청 비율·최소 동시 접속·GPU 고정·선호 설정·복제 허용을 반영하며, 균형/처리량/모델 수/주 모델 전략별로 실제 VRAM·KV cache·처리량 계산을 다시 수행
- 배치 결과에 정확/제한 근사 탐색 여부, 탐색 상태 수, GPU별 가중치·KV·런타임·안전 여유·남은 VRAM 막대, 자동 설정 변경 표시를 추가. 불균형·목표 미달 시 모델 이동, 양자화 허용, 컨텍스트 축소, 병목 모델 제외, 목표 조정을 적용 전후 VRAM·동시 접속·처리량과 함께 제시하고 버튼으로 즉시 적용
- 독립 서비스에서만 요청 비율 기반 모델 복제를 허용하고, 복제가 전체 용량을 실제로 개선하면서 기존 최소 동시 접속 제약을 악화시키지 않을 때만 채택. 새로운 배치 설정과 모델별 제약은 공유 URL에 저장·복원되며 공유 링크 진입 시 배치 결과를 자동 계산
- 목표 동시 사용자를 1/2/4/8/16/32/64명 프리셋 외에 "직접" 선택 시 최대 256명까지 숫자로 직접 입력할 수 있도록 변경 (배치 계산 자체는 이미 256명까지 지원했으나 프리셋 드롭다운이 32명에서 막혀 있었음)
- 영어 모드에서 여러 GPU 배치 결과·"3개 배치안 비교"·"실행 설정 내보내기"(실행 명령어 .sh, docker-compose 초안)가 한글로 남거나 일부 단어만 바뀐 채 섞여 나오던 문제 수정. 원인은 이 세 섹션의 렌더 함수 자체는 이미 언어별로 올바르게 문장을 만들고 있었지만, 언어 전환 시 다시 그려지지 않아 전환 전(한국어) DOM이 그대로 남아있었고, 그 위에 실행되는 범용 단어 치환이 일부 단어만 바꿔 섞인 문장으로 만들었기 때문(벤치마크 시트에서 이미 고쳤던 것과 동일한 유형의 버그). 언어 전환 시 배치 결과·배치안 비교를 다시 그리도록 수정하고, 지금까지 아예 번역되지 않던 실행 명령어(.sh)·docker-compose.yml 초안의 주석과 "복사"/"다운로드"/"복사됨" 버튼 문구도 영어를 추가
- 위 수정 검증에 사용한 jsdom 스모크 스크립트로 GPU 2대에 모델 2개를 배치하고 언어를 전환해 결과·비교 카드·내보내기 패널에 한글이 전혀 남지 않는지 확인 (임시 스크립트는 커밋하지 않음)
- 영어 모드에서 모델 상세 패널(빠른 추천 아코디언·전체 모델 탐색 드로어/인스펙터 공용)에 한글이 광범위하게 남아있던 문제 수정. 이 패널은 지금까지 한국어 문자열을 그대로 렌더링한 뒤 화면 전체에 적용되는 범용 단어 치환기가 사후에 번역하는 구조였는데, 이 패널 안의 문장 상당수가 애초에 치환기 사전에 등록되지 않았거나("동시 처리 용량(베타)" 섹션 전체, VRAM 상세/판정 근거의 "가용 VRAM"·"사용률"·"계산 조건" 등 값과 분리된 라벨, 신뢰도/추정 오차 설명 문장 등), 등록되어 있어도 숫자·GPU명 등과 결합된 동적 문장이라 부분 치환으로 "Free VRAM을", "총 Expected throughput" 같이 단어 일부만 바뀐 채 섞여 나왔음(라이선스 안내 문구의 "직접 확인하세요"가 "Custom 확인하세요"로 바뀌는 등 다른 문맥의 사전 항목과 충돌하는 사례도 있었음). 생성형/비생성형(임베딩·리랭커·OCR·VLM) 상세 패널 본문과 이들이 호출하는 동시 처리 용량, 판정 근거, 신뢰도·근거 구분, 라이선스, VRAM 예산, 실행 방식 비교, 양자화/정밀도 품질 라벨, 실행 명령어 예시 주석(모델 계열별 21개) 등 모든 하위 렌더 함수를 실제 언어별 조건 분기로 재작성해 사전 치환에 의존하지 않도록 함. jsdom으로 생성형·임베딩·리랭커·문서 VLM 4종 모델 상세를 실제 클릭 흐름(render() 경유)으로 렌더링해 영어 모드에서 잔여 한글이 없는지 확인

- 여러 GPU 배치 추천에 "3개 배치안 비교" 버튼 추가. 지금까지는 "배치 기준" 토글로 한 번에 한 가지 배치 결과만 볼 수 있었는데, 버튼을 누르면 같은 선택 모델을 균형형(균형 우선)·처리량형(신규 "처리량 우선" 기준)·모델 보존형(모델 수 우선) 세 가지로 각각 계산해 공통 동시 접속·총 예상 처리량·배치 못한 모델 수를 카드로 나란히 비교하고, 카드마다 "이 배치안 적용" 버튼으로 바로 전환 가능. "처리량 우선"은 기존 균형/압축 배치처럼 남은 VRAM 크기만 보고 GPU를 고르는 대신, 모델을 배치할 때마다 실제 동시 접속/처리량 추정 함수를 다시 호출해 그 모델에게 가장 높은 처리량을 주는 GPU를 선택하는 새 배치 기준
- 여러 GPU 배치 추천에 "운영 방식"·"목표 동시 사용자"·"주 모델" 설정 추가. (1) "운영 방식" 3단 토글(파이프라인/독립 서비스/대체 모델)로 선택한 모델들을 어떻게 쓸지 지정: "파이프라인"은 기존처럼 모델들이 하나로 이어져 가장 여유가 적은 모델이 전체를 정하는 방식(예: 임베딩→리랭커→LLM), "독립 서비스"(신규 기본값)는 각 모델을 서로 다른 사용자가 따로 호출하는 별도 서비스로 보고 하나의 공통 수치 대신 모델별 동시 접속 여유를 각각 보여줌, "대체 모델(순차 실행)"은 한 번에 모델 하나만 올려 교체해가며 쓰는 경우로 모델끼리 VRAM을 나눠 쓰지 않고 각자 GPU 전체를 기준으로 독립 계산(기존 배치 카드 대신 모델별로 어느 GPU에서 몇 명까지 가능한지 보여주는 별도 화면). (2) "목표 동시 사용자"(1/2/4/8/16/32명)를 설정하면 배치 결과와 진단이 "0명이냐 아니냐"뿐 아니라 목표 대비 부족 여부까지 비교해 알려줌. (3) "주 모델(우선 배정)"을 지정하면 배치 순서와 무관하게 그 모델이 가장 여유 있는 GPU를 먼저 차지하도록 우선 배정(결과 카드에 "주 모델" 배지 표시)
- 벤치마크 시트에 "지표로 비교할 모델 선택" 드롭다운 추가. 지금까지는 체크박스로 아무 행이나 골라 비교 그래프를 만들 수 있었는데, 이 앱의 벤치마크 데이터는 대부분 모델마다 측정된 지표가 달라(어떤 모델은 MMLU, 어떤 모델은 MMLU-Pro 등) 서로 다른 지표를 가진 행끼리 체크하면 지표별 그룹이 1개짜리로 쪼개져 사실상 비교가 안 되는 경우가 많았음. 새 드롭다운은 2개 이상의 모델이 실제로 측정된 지표만 모델 수와 함께 보여주고(예: "MMLU-Pro (49개)"), 하나를 고르면 그 지표를 가진 모델을 값 높은 순으로 최대 6개까지 자동으로 체크해 항상 실제 비교가 되는 그래프를 보여줌
- 여러 GPU 배치 추천 기능을 진단 기반으로 개선. (1) 배치 알고리즘이 지금까지 항상 "가장 빈 공간이 적게 남는 GPU부터 채우기(Best-Fit)"만 사용해 한 GPU에 모델이 몰리고 다른 GPU는 여유가 많이 남는 불균형이 자주 발생했음(예: GPU1 여유 0.8GB인데 GPU2는 14GB). "배치 기준" 토글을 새로 추가해 기본값을 "균형 우선"(Worst-Fit, GPU 간 여유를 고르게 분산)으로 바꾸고, 기존 동작은 "모델 수 우선"으로 유지. (2) 배치 결과 위에 진단 박스를 추가: GPU 간 여유 격차가 크거나 공통 동시 접속 권장치가 0명이면, 다른 배치 기준으로 바꿨을 때의 실제 재계산 결과(여유 격차·공통 동시 접속 변화)를 보여주며 바로 전환할 수 있는 버튼을 제공하고, 전략을 바꿔도 도움이 안 되면 어떤 모델을 어느 GPU로 옮기면 좋을지 실제 추정 함수로 재계산한 구체적 제안을, 그것도 여의치 않으면 모델 축소·양자화/컨텍스트 조정·GPU 추가 등 일반 안내를 보여줌. 모든 수치는 실제 VRAM/동시 접속 추정 함수를 다시 호출해 계산한 값이며 하드코딩된 값이 아님. (3) 공통 동시 접속 권장치가 0명일 때 일반 안내문처럼 보이지 않도록 경고 스타일(빨간 텍스트·배지)과 "운영 비권장" 문구로 명확히 구분
- 한/영 언어 토글 옆에 다크 모드 토글("라이트"/"다크")을 새로 추가. 배경·본문색·테두리·강조색·pill/배지·VRAM 메모리 맵 막대·벤치마크 그래프 막대 그라디언트 등 스타일시트 전반의 고정 색상 값을 CSS 커스텀 프로퍼티로 정리해 `[data-theme="dark"]` 한 벌만으로 전체 화면이 전환되도록 함(항상 어두운 배경을 쓰는 코드 블록은 제외). 선택한 테마는 브라우저에 저장되어 다음 방문 때도 유지되며, 저장된 값이 없으면 운영체제의 다크 모드 설정을 따름. `<head>`에 렌더링 전 테마를 미리 적용하는 짧은 인라인 스크립트를 추가해 다크 모드 사용자가 페이지를 열 때 흰 화면이 잠깐 번쩍이는 현상도 방지
- 영어 모드에서 벤치마크 시트 그래프의 안내문·지표 설명·"OOO이(가) 가장 높습니다" 자동 분석 문장이 번역되지 않고 한글로 남거나("초당 처리 pages 수"처럼) 단어 일부만 영어로 바뀐 채 섞여 나오던 문제 수정. 원인은 이 문장들이 완전한 자연어 문장이라 단어 단위 사전 치환으로는 애초에 옳게 번역할 수 없어 전용 한/영 함수로 따로 만들어 뒀는데, 언어 전환 버튼을 눌렀을 때 벤치마크 시트가 다시 그려지지 않아 언어를 바꾸기 전 화면(항상 한국어)이 그대로 남아있었기 때문. 여기에 이후 실행되는 범용 단어 치환이 일부 단어만 부분적으로 바꿔 섞인 문장이 된 것. 언어 전환 시 벤치마크 시트를 다시 그리도록 수정
- 전반적으로 "AI 생성 툴 느낌"이 난다는 피드백에 따라 UI 폴리시 진행: (1) 헤더 로고 위 "AI Hardware Fit", 시작 화면·빠른 추천·벤치마크 시트 섹션마다 반복되던 "작은 대문자 라벨(kicker) + 굵은 제목 + 회색 설명" 3단 구성에서 라벨 줄을 제거해 섹션마다 똑같은 틀로 찍어낸 듯한 느낌을 줄임(값 위의 실제 기능 라벨인 "내 실행 환경"은 유지). (2) 빠른 추천 카드 3개가 배지 색만 다르고 크기·형태가 완전히 동일해 1순위가 눈에 띄지 않던 것을, 1순위 카드에 파란 배경 틴트와 굵은 테두리를 항시 적용하고 모델명 글자도 살짝 키워 실제로 구분되게 함. (3) 1/2/3순위 배지를 채워진 파란 알약(pill) 모양에서 일반 색상 텍스트로 바꾸고, 빠른 추천 우측 상단의 "90 GPU 프리셋 · 290 AI 모델 · 283 출처 연결 평가" 배지 3개도 각각 테두리 박스였던 것을 가운데 점(·)으로 구분한 한 줄 텍스트로 정리해 알약형 요소 남용을 줄임
- 영어 모드에서 리랭커 설정 요약("질의 64 + 문서 512 · 후보 40개"), 후보/배치/동시 요청/OCR 배치 프리셋 드롭다운("40개", "16쌍", "1명", "32개", "1페이지" 등), 여러 GPU 배치 추천의 빈 상태 안내문("동시에 띄울 모델을 하나 이상 선택해주세요")이 번역되지 않고 한글로 남거나 일부만 번역되던 문제 수정. 프리셋 드롭다운은 같은 "숫자+개" 형태가 임베딩 배치·리랭커 후보 등 서로 다른 프리셋에서 다른 의미로 쓰여 공용 치환 규칙으로는 안전하게 옮길 수 없어서, 프리셋별로 정확한 라벨을 직접 대응시키는 방식으로 처리(단수 값은 "1 pair"/"1 text"/"1 page"처럼 단수형으로도 표시). 부수적으로, 언어를 영어로 바꿨다가 다시 한국어로 되돌릴 때 "Text Embeddings Inference" 같은 고유 명칭이 "Text 임베딩s Inference"처럼 깨지던 원인(짧은 치환 규칙이 더 긴 영단어의 접두어까지 물어가던 경계 처리 누락)도 함께 수정

- 목록 정렬의 "최신 모델순"이 실제로는 모델명 문자열에 하드코딩된 키워드 매칭 목록(`modelFreshnessScore`)으로 점수를 매기고 있어, LLM 일부는 그럭저럭 맞았지만 임베딩·리랭커·OCR/VLM은 목록 갱신 때마다 빠짐없이 챙기지 못해 실제 출시일과 무관하게 섞여 나오던 문제 수정. 모든 모델에 이미 등록돼 있는 실제 `releaseDate` 값으로 직접 비교하도록 교체해 생성형·임베딩·리랭커·OCR/VLM 전부 동일하게 정확히 정렬됨. 아울러 전체 모델 탐색 목록의 기본 정렬을 "종합 추천"에서 "최신 모델순"으로 변경해 처음 들어왔을 때 가장 최근 나온 모델이 위에 오도록 함
- 벤치마크 시트 표에서 GPU·조건 두 열이 품질 벤치마크(대다수 행)에는 하드웨어와 무관해 항상 빈칸/중복 문구였던 것을 "환경" 한 열로 합치고, 확보한 공간에 "규모/출시"(파라미터 수 + 출시일) 열을 새로 추가해 표만 보고도 모델 크기·최신성을 바로 비교할 수 있도록 함
- 벤치마크 시트 그래프 막대에 파란 채움 색이 전혀 안 보이던 버그 수정: 채움을 그리는 `<span>`이 부모 트랙 `<span>` 안에 중첩돼 있어 (트랙 자체는 CSS grid 항목이라 자동으로 block 처리되지만, 그 안의 채움 span은 grid 항목이 아니라서 여전히 inline으로 남음) JS가 넣어준 `width:NN%` 인라인 스타일이 브라우저에 의해 그냥 무시되고 있었음. 채움 span에 `display: block` 추가로 해결. 아울러 그래프 오른쪽에 남던 빈 공간을 활용해 1400px 이상 넓은 화면에서는 벤치마크 표를 그래프 아래가 아니라 오른쪽 옆에 나란히 배치하도록 변경 (좁은 화면에서는 기존처럼 세로로 쌓임)
- 빠른 추천 카드를 인라인 아코디언으로 바꾼 뒤(위 항목) CI가 계속 실패하던 문제 수정: 기존 테스트가 카드를 직접 클릭하면 전체 모델 탐색 드로어(`#modelDetail`)가 열린다는 예전 동작을 그대로 가정하고 있었음. 테스트를 새 구조(펼침 버튼 클릭 → 카드 자체가 `.is-expanded`로 확장 → 내부에 공유 링크·요약 카드 다운로드 버튼 노출)에 맞게 갱신. 앱 동작 자체는 변경 없음, 낡은 테스트만 실제 동작에 맞춤
- 벤치마크 시트 그래프의 막대 트랙이 `1fr`로 화면 폭만큼 늘어나 값이 차 있어도 휑해 보이던 문제 수정. 트랙 폭을 140~320px로 제한하고 두께를 키우며 25% 단위 눈금선을 추가해 "100점 중 지금 여기까지 찼다"는 게 한눈에 보이도록 함
- 생성형 LLM에 DeepSeek V4-Pro(1.6T·49B 활성), DeepSeek V4-Flash(284B·13B 활성, 둘 다 MIT·1M 컨텍스트), Solar Open 2 250B A15B(업스테이지, 하이브리드 선형-소프트맥스 어텐션 MoE·1M 컨텍스트·한국어 강점) 추가. 문서 VLM에 GLM-OCR(Z.ai, 0.9B·OmniDocBench v1.5 94.62로 1위) 추가. Solar Open 2의 라이선스("Upstage Solar License": 가중치 그대로는 상업 이용 가능, 파생 모델은 'Solar' 접두사·"Built with Solar" 표기 필요)를 신규 라이선스 정책으로 등록. 벤치마크 시트의 기존 OmniDocBench 관련 5개 항목이 "96.33"처럼 숫자만 있는 라벨이라 그래프에서 지표별로 묶이지 않던 문제도 "OmniDocBench v1.5/v1.6 96.33" 형태로 함께 수정
- 전체 모델 탐색(전문가) 모드에서 데스크톱 인스펙터 패널(1536px 이상)이 모델을 아직 선택하지 않았을 때도 항상 공간을 차지해, 같은 화면 폭에서도 빠른 추천보다 목록·양자화 카드가 좁아 보이던 문제 수정. 이제 모델을 실제로 선택했을 때만 오른쪽 패널 공간을 확보하고, 아무것도 선택하지 않았을 때는 빠른 추천과 동일하게 전체 폭을 사용
- 벤치마크 시트 그래프의 막대를 "현재 선택된 항목들 중 최댓값" 기준 상대 길이에서, 지표별 실제 만점 기준(대부분 0~100점, MT-Bench·LogicKor 0~10점, OCRBench 0~1000점 등)의 절대 길이로 변경. 값이 서로 비슷한 모델들만 선택했을 때 막대가 전부 꽉 찬 것처럼 보이던 문제를 해결하고, 숫자 옆에 만점 기준(예: `87.8/100`)을 함께 표시. tok/s·doc/s·pair/s·page/s처럼 정해진 만점이 없는 처리량 지표는 기존처럼 선택 항목 중 최댓값 기준 상대 길이 유지
- 벤치마크 시트 그래프의 각 지표 그룹 제목 아래에 MMLU-Pro·BEIR·tok/s 등 지표가 무엇을 측정하는지 한 줄 설명을 추가 (지원하는 지표: MMLU-Pro, MMLU, MMMLU, KMMLU, GPQA(-D), HumanEval, MBPP, AIME24/25, MATH, GSM8K, BEIR, NDCG@10, MTEB, IFEval, MT-Bench, LogicKor, Arena Hard, OCRBench(v2), DocVQA, MMMU, Korean Avg, Avg, tok/s, doc/s, pair/s, page/s)
- 빠른 추천 카드를 클릭하면 목록을 가리는 드로어가 열리던 것을, 클릭한 카드 바로 아래에 상세 내용이 펼쳐지는 인라인 아코디언 방식으로 변경. 나머지 카드는 자리 그대로 남고, 펼쳐진 카드는 표 전체 폭으로 확장되어 VRAM 분석·양자화 비교·실행 명령어 등 기존 드로어와 동일한 내용을 보여줌 (내용 생성 로직을 `buildGenerativeDetailBodyHtml`/`buildNonGenerativeDetailBodyHtml`로 분리해 드로어·아코디언이 같은 함수를 재사용). 다른 카드를 클릭하면 이전 카드는 접히고 새 카드가 펼쳐지며, 같은 카드를 다시 클릭하면 접힘. 전체 모델 탐색(전문가) 모드의 드로어/인스펙터 동작은 변경 없음 (별도 상태 변수로 분리)
- 벤치마크 시트 그래프에서 아무 행도 선택하지 않았을 때 빈 화면 대신, 가장 많이 등장하는 지표 상위 3개를 기본으로 보여주도록 변경. 각 지표 그룹 아래에 "OOO 기준 △△△이(가) 가장 높습니다/빠릅니다 (최저 대비 +NN%)" 같은 한 줄 요약 분석을 자동 생성해 추가. 행을 체크하면 여전히 선택한 모델로만 그래프가 바뀜
- 벤치마크 시트(페이지 최하단)에 검색창과 다중 선택 비교 그래프 추가. 모델명·GPU·지표로 실시간 검색해 행을 좁힐 수 있고, 각 행 왼쪽 체크박스로 최대 6개까지 선택하면 선택된 행만 모아 지표(품질 지표명 또는 tok/s·doc/s·pair/s·page/s)별로 그룹핑한 가로 막대 그래프를 표 아래에 렌더링. 서로 다른 지표는 절대 같은 막대 그래프에 섞지 않고, 지표 그룹별로 자체 최대값 기준 상대 막대 길이를 계산. 선택 초기화 버튼으로 비교 상태를 즉시 비움
- 전체 모델 탐색(전문가) 모드에서 "양자화별 추천" 카드 줄이 가로 스크롤로 잘려 보이던 문제 수정: 카드 목록을 `overflow-x: auto` 한 줄 스크롤에서 `flex-wrap`으로 바꿔 화면 폭에 맞게 여러 줄로 자연스럽게 흘러가도록 변경
- 전체 모델 탐색(전문가) 모드의 모델 상세를 넓은 화면(1536px 이상)에서 오른쪽 고정 인스펙터 패널로 전환. 지금까지는 모델을 클릭하면 목록을 가리는 오버레이 드로어가 열렸는데, 이제 넓은 화면에서는 목록 오른쪽에 항상 떠 있는 패널에서 모델을 바꿔가며 VRAM 구성·여유율·속도 근거를 계속 확인할 수 있음. 빠른 추천 모드와 1536px 미만 화면(태블릿·노트북)은 기존 오버레이 드로어 그대로 유지. 아무 모델도 선택하지 않았을 때는 빈 흰 패널 대신 안내 문구를 표시. 목록에 이미 있던 선택 행 강조(파란 왼쪽 선)는 그대로 재사용
- 위 인스펙터 패널을 위해 목록의 "출시/세대"·"권장 설정" 두 열은 인스펙터 패널에서 바로 확인 가능하므로 인스펙터가 열리는 화면 폭에서만 숨김(전체 모델 탐색 목록 자체나 좁은 화면에서는 그대로 노출). 원래 계획한 1280px 기준은 목록의 고정폭 열 10개 합이 이미 850px를 넘어 360~520px 인스펙터와 함께 들어가지 않아 1536px로 조정
- 계산 로직·DOM ID·필터·비교 기능은 변경 없음. jsdom으로 모드 전환 시 body 클래스 토글, 모델 선택 시 인스펙터 내용 갱신과 선택 행 강조, 상세 닫기 시 안내 문구 복귀를 확인

- 생성형 LLM 147종, 임베딩 60종, 리랭커 34종, OCR/VLM 45종 등 등록된 모델 286개 전체에 실제 영문 요약 데이터 추가. 지금까지 영어 모드에서는 모델별 한국어 설명 대신 "language model for {태그}. The Korean description is available in the Korean interface." 같은 일반 문장으로 대체됐는데, 이제 `summary` 필드를 `{ ko, en }` 형태로 바꾸고 기존 한국어 설명을 그대로 번역해 넣어 영어 사용자도 모델별로 실제 설명을 봄. 같은 방식으로 라이선스 정책 31종(고유 요약 22종)의 `summary`도 `{ ko, en }`로 전환해 영어 모드에서 일반 문구 대신 실제 라이선스 조건 설명이 보이도록 함. 데이터 유효성 검사(`npm run check`의 validate-data)와 jsdom 검증으로 전부 객체 형태로 반영됐는지, 언어 전환 시 정상적으로 전환되는지 확인
- GPU를 아직 선택하지 않은 첫 방문 화면을 컴팩트한 드롭다운 한 줄에서 전용 시작 화면으로 교체: RTX 5090/4090/4080 SUPER/4070 Ti SUPER/4060 Ti/3090/3060, Radeon RX 7900 XTX 등 자주 찾는 GPU 카드 8개를 VRAM·대역폭과 함께 큼직하게 보여주고, 그 아래 GPU 프리셋 전체(90개)를 검색해 바로 선택할 수 있는 입력창을 배치. GPU를 고르기 전까지는 하드웨어 설정 패널·결과 목록·벤치마크 시트를 숨겨 화면이 산만해지지 않게 하고, 선택 즉시 기존 계산 화면으로 전환
- GitHub Pages 배포가 `push`로 CI와 동시에 시작되어 CI가 실패해도 이미 배포가 끝나 있을 수 있던 문제 수정: `CI` 워크플로가 main에서 성공적으로 끝난 뒤에만 `workflow_run`으로 배포를 트리거하도록 변경
- 전반적인 UI를 "Light Industrial Benchmark Workbench" 방향으로 재도색: 흰 배경/파란 강조/둥근 카드/pill 배지가 반복되던 기존 톤에서 벗어나, 따뜻한 회백색 캔버스, 하드코딩 없이 `--radius-sm/--radius-md` 토큰으로 통일된 3~5px 모서리, 대부분의 카드 그림자 제거(모달·드로어처럼 실제로 떠 있는 요소만 얇은 그림자 유지)로 정리. 등급 표시는 채워진 pill 대신 점(dot) + 텍스트로, VRAM·속도 등 수치 열은 모노스페이스 고정폭 폰트로 변경
- 모델 상세 패널에 VRAM 메모리 맵(가중치/KV cache/런타임/여유를 하나의 막대로 시각화) 추가. 생성형 LLM과 임베딩·리랭커·OCR/VLM 등 비생성형 모델 상세 모두에 동일하게 적용
- 벤치마크 시트에 근거 출처를 나타내는 짧은 코드 배지(EST 계산 추정 · EXT 외부 공개 참고값 · USER 사용자 측정 · SELF 자체 측정) 추가
- 빠른 추천 카드에서 임베딩·리랭커·OCR/VLM 모델을 추천했을 때 "Ollama 복사" 버튼이 생성형 전용 명령어 생성 함수를 호출해 예외로 죽던 버그 수정. 모델 종류에 맞는 실행 명령어를 생성하도록 분기하고 버튼 라벨도 "실행 명령어 복사"로 일반화
- 영어 UI를 키 기반 메시지(`t(key)`)로 전환하기 시작하고, 재렌더링되는 설정·필터·빠른 추천·벤치마크 문구도 현재 언어로 생성하도록 수정. `?lang=en` 공유 URL과 영어 레이아웃 폭 보정 추가
- 언어 선택 UI를 지구본 아이콘 + 드롭다운에서 헤더 다른 링크들과 어울리는 컴팩트한 `한국어 | EN` 필 토글로 정리
- 영어 모드에서 곳곳이 한국어로 남거나 "Model을", "Search해서 선택"처럼 단어 일부만 바뀌어 깨져 보이던 문제 수정. 번역 치환이 한글 단어 중간을 잘라 붙이지 않도록 경계 검사를 추가하고, 설정 항목 라벨·결과 테이블 헤더·태그·라이선스 문구·추천 사유·벤치마크 시트·비교 모달·고급 도구 안내문 등 번역 누락 범위를 대폭 확충 (약 600개 → 30여 개 잔여, 남은 항목은 GPU 프리셋 설명과 동시 요청/배치 크기 등 일부 드롭다운 옵션 단위 표기 정도)
- 빠른 추천과 모델 상세에 결과 링크 복사 및 GPU·추천 모델·VRAM·추정 속도 요약 카드 PNG 다운로드 추가. 빠른 추천 용도·우선순위도 공유 URL에 저장·복원
- 상세 설정을 `기본 하드웨어 · 보조 GPU · 메모리 보정` 3개 밀집형 그룹으로 재배치하고 입력 높이·간격을 축소. 접힌 고급 도구는 데스크톱에서 2열로 표시하고, 워크로드별 실행 조건도 넓은 화면에서 한 줄 중심으로 정리
- 첫 방문 시 임의의 RTX 4090 결과를 바로 보여주지 않고 GPU를 한 번 명시적으로 선택하도록 변경. 선택한 고정 GPU는 브라우저에 기억하며, 이후에는 빠른 추천을 기본 화면으로 열어 용도·우선순위에 맞는 상위 3개 모델과 상세 계산 진입점을 먼저 표시. GPU 외 VRAM·RAM·동시 요청·고급 도구는 `상세 설정` 안에 유지
- 빠른 추천 모드에서 상단의 상태별 개수(전체/쾌적/잘 돌아감/가능 등) 칩이 계속 보이고 클릭도 됐지만, 이 칩은 전체 모델 탐색 목록의 필터라 빠른 추천에서는 눌러도 화면에 아무 변화가 없어 "안 눌린다"처럼 보이던 문제 수정. 이제 이 칩은 전체 모델 탐색 모드에서만 보이고, 빠른 추천에서 실수로 필터를 걸어둔 채 전체 모델 탐색으로 넘어가 목록이 미리 걸러져 있던 문제도 함께 해결
- GitHub Issue 생성 정책이 `ALL`이고 상호작용 제한이 없음을 확인한 뒤 모델·GPU·벤치마크 제보 링크 재개
- GitHub 저장소 About 설명과 Topics를 LLM·임베딩·리랭커·OCR·VLM·멀티 GPU 범위에 맞게 갱신

## v1.1.0 - AI Workload Expansion - 2026-07-24

- 하드웨어 설정 패널을 페이지 진입 시 기본적으로 펼쳐서 보여주도록 변경 (매번 "설정 변경"을 눌러야 GPU를 바꿀 수 있던 문제)
- "고급 도구"(Hugging Face 직접 계산 · 여러 GPU 배치)를 결과 목록 맨 아래에서 GPU 설정 패널 안으로 이동. 전체 모델 탐색 모드에서 모델 목록(최대 147개 행)을 다 스크롤해야만 닿을 수 있어 사실상 눈에 띄지 않던 문제를 해결
- 페이지 흐름 재구성: 워크로드 탭 위에 `빠른 추천 / 전체 모델 탐색` 모드 전환을 추가해 초보자 모드가 전체 목록 사이에 끼어 흐름을 끊지 않도록 최상위 모드로 분리. Hugging Face 직접 계산·여러 GPU 배치는 결과 목록 아래 "고급 도구" 아코디언으로 이동. 정렬 드롭다운을 검색·필터와 한 줄로 통합
- 모델 비교 모달 레이아웃 정리: 모델별 열 사이 구분선 추가, 비교 제외 버튼을 헤더 우측 상단으로 이동, 상단에 실행 가능 여부 요약 문장 추가, 평가 기준이 다른 대표 공개 평가는 "직접 비교 불가" 표시, 필요 VRAM·추정 속도 차이를 색상으로 강조
- 여러 GPU 배치 계산 결과에 "실행 설정 내보내기" 추가: 배치된 모델 전체의 실행 명령어(.sh)와 Ollama·TEI 기준 docker-compose.yml 초안을 복사/다운로드 가능. OCR/VLM처럼 실행 방식이 모델마다 다른 경우는 compose 자동 생성 대신 안내 주석과 .sh 명령어로 대체
- 초보자 모드(3단계 추천) 추가: 현재 선택된 GPU 기준으로 용도·우선순위 2가지만 고르면 현재 탭에서 실행 가능한 모델 중 상위 3개를 추천 이유와 함께 보여줌. 기존 전체 목록은 그대로 "전문가 모드"로 유지
- 모델 목록에서 체크박스로 최대 3개 모델을 선택해 등급·VRAM·추정 속도·대표 공개 평가·라이선스·태그를 표 형태로 나란히 비교하는 비교 모드 추가 (목록/카드 보기 모두 지원)
- README를 ~180줄의 홍보용 요약 페이지로 축소하고, 계산 수식·정확도/한계·참고 자료를 `docs/methodology.md`, `docs/accuracy-and-limits.md`, `docs/data-sources.md`로 분리
- README를 출시용 요약으로 다시 축소하고 `README.en.md` 및 10초 데모 GIF 추가
- 공개 Issue 생성 제한 복구 확인 전까지 README의 모델·GPU·벤치마크 제보 흐름을 일시 중단으로 표시
- 계산값을 계산 추정·외부 공개 참고값·사용자 측정·자체 측정으로 명확히 구분
- 릴리스 태그에서 GitHub Release를 발행하는 검증 워크플로 추가

- 루프백 전용 로컬 벤치마크 CLI(`scripts/benchmark-cli.mjs`) 추가: Ollama/llama.cpp에 실제 프롬프트를 보내 prefill/decode 속도·TTFT·peak VRAM을 사용자 측정 JSON으로 생성
- Benchmark report 이슈 템플릿에 CLI 출력 JSON을 바로 붙여넣는 칸 추가
- 모델 상세에 동일 조건 사용자/자체 측정값이 있을 때 "예상 vs 측정 · 추정 오차 %"를 표시
- 배치 계산 버튼 옆에 배치된 모델 전체의 공통 병목 기준(동시 접속 인원·처리량) 요약 뱃지 추가
- 기본·보조 GPU와 여러 GPU 배치 인벤토리는 드롭다운을 기본으로 사용하고, 검색 옵션을 선택하면 모델명 자동완성 입력을 표시하도록 변경
- 모델 상세에 동시 처리 용량(베타) 추가: GPU·모델·양자화·컨텍스트 기준 권장/최대 동시 인원과 총·1인당 처리량을 KV cache 기준으로 역산
- 여러 GPU에 모델 동시 배치 추천(베타) 추가: 검색 가능한 체크리스트로 생성형 LLM·임베딩·리랭커·OCR/VLM을 섞어서 선택하면 GPU별 모델·정밀도 배치와 병목 없는 동시 인원·처리량, 배치 불가 모델을 계산
- 모델 라이선스 안내 추가: 상업 이용 가능/조건부/비상업·연구용/원문 확인 필요를 모델별 원문 링크와 함께 표시
- 이기종 GPU 병렬(서로 다른 GPU 두 대의 VRAM·대역폭·연산량 합산, 메모리 분할·통신 손실 반영) 지원 추가
- 요청받은 NVIDIA P102-100 10GB(채굴카드) GPU 프리셋 추가
- 리랭커·LLM·임베딩·OCR/VLM 모델 164종에 출처 연결 공개 품질 벤치마크 수치 추가
- 첫 화면을 카드형 중심에서 목록형 빠른 비교 화면으로 개편
- README Mermaid 다이어그램을 제거하고 자동 양자화 수식 렌더링 오류 수정
- 하드웨어 입력과 고급 설정을 접힘 영역 없이 기본 노출되도록 변경
- 브라우저 보안 제약으로 정확도가 낮은 PC 자동 감지 기능 제거
- 생성형 LLM, 임베딩, 리랭커, 경량 OCR, 문서 VLM, 범용 VLM 탭 추가
- 임베딩 모델 60종, 리랭커 34종, OCR/VLM 모델 45종 추가
- EmbeddingGemma, Jina Embeddings v4, BGE multilingual Gemma2/code/en-ICL, GTE-Qwen2, E5-Mistral, SFR, Granite R2, Stella, Nomic, KURE/KoE5/dragonkue 한국어 임베딩 추가
- bge-reranker-v2.5-gemma2-lightweight, mxbai-rerank v2, gte-reranker-modernbert, Granite reranker R2, Jina v1, BCE, 한국어 리랭커, Sentence Transformers cross-encoder 기준선 추가
- PaddleOCR-VL-1.6, MinerU2.5-Pro, DeepSeek-OCR-2, dots.ocr, dots.mocr, olmOCR-2, DeepSeek-VL2, Qwen3-VL, Qwen2.5-VL, Qwen2-VL, Llama 3.2 Vision, Pixtral, LLaVA-OneVision, Molmo, SmolVLM2, Phi-4 multimodal, Aya Vision, GLM-4.1V, MiniCPM-V, InternVL3.5, Kimi-VL 추가
- 임베딩 encoder FLOPs/activation, 리랭커 query latency, OCR 해상도/megapixel 기반 계산식 추가
- 모든 워크로드에 공통 적용되는 이미 사용 중인 VRAM/안전 여유분 입력과 가용 VRAM 기준 등급 계산 추가
- 컨텍스트, 동시 요청, 출력 토큰, RAG 배치, OCR 배치 페이지에 프리셋과 직접 입력을 함께 제공
- H100/A100 같은 데이터센터 GPU를 빠르게 찾을 수 있도록 GPU 프리셋 검색 입력 추가
- 적합도 요약을 클릭 가능한 필터 칩으로 변경
- 모델 클릭 시 양자화별 비교, VRAM 상세 분석, 실행 방식별 비교, 예시 명령어를 보여주는 상세 패널 추가
- GPU/VRAM/RAM/컨텍스트/필터/선택 모델을 URL 쿼리로 공유할 수 있도록 변경

## v1.0.0 - First public release

- GPU 프리셋 86종
- LLM 모델 114종
- 양자화 옵션 8종
- WebGPU/WebGL 기반 GPU 자동 감지 및 프리셋 추정 매칭
- Ollama, llama.cpp, vLLM, Transformers 조건별 메모리 추정
- 컨텍스트 길이, 동시 요청 수, 평균 출력 토큰, KV cache 정밀도 반영
- GitHub Pages 공개 데모 준비
- 모델/GPU/벤치마크 제보용 Issue template 추가
