# Changelog

## Unreleased

- 한/영 언어 토글 옆에 다크 모드 토글("라이트"/"다크")을 새로 추가. 배경·본문색·테두리·강조색·pill/배지·VRAM 메모리 맵 막대·벤치마크 그래프 막대 그라디언트 등 스타일시트 전반의 고정 색상 값을 CSS 커스텀 프로퍼티로 정리해 `[data-theme="dark"]` 한 벌만으로 전체 화면이 전환되도록 함(항상 어두운 배경을 쓰는 코드 블록은 제외). 선택한 테마는 브라우저에 저장되어 다음 방문 때도 유지되며, 저장된 값이 없으면 운영체제의 다크 모드 설정을 따름. `<head>`에 렌더링 전 테마를 미리 적용하는 짧은 인라인 스크립트를 추가해 다크 모드 사용자가 페이지를 열 때 흰 화면이 잠깐 번쩍이는 현상도 방지
- 영어 모드에서 벤치마크 시트 그래프의 안내문·지표 설명·"OOO이(가) 가장 높습니다" 자동 분석 문장이 번역되지 않고 한글로 남거나("초당 처리 pages 수"처럼) 단어 일부만 영어로 바뀐 채 섞여 나오던 문제 수정. 원인은 이 문장들이 완전한 자연어 문장이라 단어 단위 사전 치환으로는 애초에 옳게 번역할 수 없어 전용 한/영 함수로 따로 만들어 뒀는데, 언어 전환 버튼을 눌렀을 때 벤치마크 시트가 다시 그려지지 않아 언어를 바꾸기 전 화면(항상 한국어)이 그대로 남아있었기 때문. 여기에 이후 실행되는 범용 단어 치환이 일부 단어만 부분적으로 바꿔 섞인 문장이 된 것. 언어 전환 시 벤치마크 시트를 다시 그리도록 수정
- 전반적으로 "AI 생성 툴 느낌"이 난다는 피드백에 따라 UI 폴리시 진행: (1) 헤더 로고 위 "AI Hardware Fit", 시작 화면·빠른 추천·벤치마크 시트 섹션마다 반복되던 "작은 대문자 라벨(kicker) + 굵은 제목 + 회색 설명" 3단 구성에서 라벨 줄을 제거해 섹션마다 똑같은 틀로 찍어낸 듯한 느낌을 줄임(값 위의 실제 기능 라벨인 "내 실행 환경"은 유지). (2) 빠른 추천 카드 3개가 배지 색만 다르고 크기·형태가 완전히 동일해 1순위가 눈에 띄지 않던 것을, 1순위 카드에 파란 배경 틴트와 굵은 테두리를 항시 적용하고 모델명 글자도 살짝 키워 실제로 구분되게 함. (3) 1/2/3순위 배지를 채워진 파란 알약(pill) 모양에서 일반 색상 텍스트로 바꾸고, 빠른 추천 우측 상단의 "90 GPU 프리셋 · 290 AI 모델 · 283 출처 연결 평가" 배지 3개도 각각 테두리 박스였던 것을 가운데 점(·)으로 구분한 한 줄 텍스트로 정리해 알약형 요소 남용을 줄임
- 영어 모드에서 리랭커 설정 요약("질의 64 + 문서 512 · 후보 40개"), 후보/배치/동시 요청/OCR 배치 프리셋 드롭다운("40개", "16쌍", "1명", "32개", "1페이지" 등), 여러 GPU 배치 추천의 빈 상태 안내문("동시에 띄울 모델을 하나 이상 선택해주세요")이 번역되지 않고 한글로 남거나 일부만 번역되던 문제 수정. 프리셋 드롭다운은 같은 "숫자+개" 형태가 임베딩 배치·리랭커 후보 등 서로 다른 프리셋에서 다른 의미로 쓰여 공용 치환 규칙으로는 안전하게 옮길 수 없어서, 프리셋별로 정확한 라벨을 직접 대응시키는 방식으로 처리(단수 값은 "1 pair"/"1 text"/"1 page"처럼 단수형으로도 표시). 부수적으로, 언어를 영어로 바꿨다가 다시 한국어로 되돌릴 때 "Text Embeddings Inference" 같은 고유 명칭이 "Text 임베딩s Inference"처럼 깨지던 원인(짧은 치환 규칙이 더 긴 영단어의 접두어까지 물어가던 경계 처리 누락)도 함께 수정

- 목록 정렬의 "최신 모델순"이 실제로는 모델명 문자열에 하드코딩된 키워드 매칭 목록(`modelFreshnessScore`)으로 점수를 매기고 있어, LLM 일부는 그럭저럭 맞았지만 임베딩·리랭커·OCR/VLM은 목록 갱신 때마다 빠짐없이 챙기지 못해 실제 출시일과 무관하게 섞여 나오던 문제 수정. 모든 모델에 이미 등록돼 있는 실제 `releaseDate` 값으로 직접 비교하도록 교체해 생성형·임베딩·리랭커·OCR/VLM 전부 동일하게 정확히 정렬됨. 아울러 전체 모델 탐색 목록의 기본 정렬을 "종합 추천"에서 "최신 모델순"으로 변경해 처음 들어왔을 때 가장 최근 나온 모델이 위에 오도록 함
- 벤치마크 시트 표에서 GPU·조건 두 열이 품질 벤치마크(대다수 행)에는 하드웨어와 무관해 항상 빈칸/중복 문구였던 것을 "환경" 한 열로 합치고, 확보한 공간에 "규모/출시"(파라미터 수 + 출시일) 열을 새로 추가해 표만 보고도 모델 크기·최신성을 바로 비교할 수 있도록 함
- 벤치마크 시트 그래프 막대에 파란 채움 색이 전혀 안 보이던 버그 수정: 채움을 그리는 `<span>`이 부모 트랙 `<span>` 안에 중첩돼 있어 (트랙 자체는 CSS grid 항목이라 자동으로 block 처리되지만, 그 안의 채움 span은 grid 항목이 아니라서 여전히 inline으로 남음) JS가 넣어준 `width:NN%` 인라인 스타일이 브라우저에 의해 그냥 무시되고 있었음. 채움 span에 `display: block` 추가로 해결. 아울러 그래프 오른쪽에 남던 빈 공간을 활용해 1400px 이상 넓은 화면에서는 벤치마크 표를 그래프 아래가 아니라 오른쪽 옆에 나란히 배치하도록 변경 (좁은 화면에서는 기존처럼 세로로 쌓임)
- 빠른 추천 카드를 인라인 아코디언으로 바꾼 뒤(위 항목) CI가 계속 실패하던 문제 수정: 기존 테스트가 카드를 직접 클릭하면 전체 모델 탐색 드로어(`#modelDetail`)가 열린다는 예전 동작을 그대로 가정하고 있었음. 테스트를 새 구조(펼침 버튼 클릭 → 카드 자체가 `.is-expanded`로 확장 → 내부에 공유 링크·요약 카드 다운로드 버튼 노출)에 맞게 갱신. 앱 동작 자체는 변경 없음, 낡은 테스트만 실제 동작에 맞춤
- 벤치마크 시트 그래프의 막대 트랙이 `1fr`로 화면 폭만큼 늘어나 값이 차 있어도 휑해 보이던 문제 수정. 트랙 폭을 140~320px로 제한하고 두께를 키우며 25% 단위 눈금선을 추가해 "100점 중 지금 여기까지 찼다"는 게 한눈에 보이도록 함
- 생성형 LLM에 DeepSeek V4-Pro(1.6T·49B 활성), DeepSeek V4-Flash(284B·13B 활성, 둘 다 MIT·1M 컨텍스트), Solar Open 2 250B A15B(업스테이지, 하이브리드 선형-소프트맥스 어텐션 MoE·1M 컨텍스트·한국어 강점) 추가. 문서 VLM에 GLM-OCR(Z.ai, 0.9B·OmniDocBench v1.5 94.62로 1위) 추가. Solar Open 2의 라이선스("Upstage Solar License": 가중치 그대로는 상업 이용 가능, 파생 모델은 'Solar' 접두사·"Built with Solar" 표기 필요)를 신규 라이선스 정책으로 등록. 벤치마크 시트의 기존 OmniDocBench 관련 5개 항목이 "96.33"처럼 숫자만 있는 라벨이라 그래프에서 지표별로 묶이지 않던 문제도 "OmniDocBench v1.5/v1.6 96.33" 형태로 함께 수정
- 전체 모델 탐색(전문가) 모드에서 데스크톱 인스펙터 패널(1536px 이상)이 모델을 아직 선택하지 않았을 때도 항상 공간을 차지해, 같은 화면 폭에서도 빠른 추천보다 목록·양자화 카드가 좁아 보이던 문제 수정. 이제 모델을 실제로 선택했을 때만 오른쪽 패널 공간을 확보하고, 아무것도 선택하지 않았을 때는 빠른 추천과 동일하게 전체 폭을 사용
- 벤치마크 시트 그래프의 막대를 "현재 선택된 항목들 중 최댓값" 기준 상대 길이에서, 지표별 실제 만점 기준(대부분 0~100점, MT-Bench·LogicKor 0~10점, OCRBench 0~1000점 등)의 절대 길이로 변경. 값이 서로 비슷한 모델들만 선택했을 때 막대가 전부 꽉 찬 것처럼 보이던 문제를 해결하고, 숫자 옆에 만점 기준(예: `87.8/100`)을 함께 표시. tok/s·doc/s·pair/s·page/s처럼 정해진 만점이 없는 처리량 지표는 기존처럼 선택 항목 중 최댓값 기준 상대 길이 유지
- 벤치마크 시트 그래프의 각 지표 그룹 제목 아래에 MMLU-Pro·BEIR·tok/s 등 지표가 무엇을 측정하는지 한 줄 설명을 추가 (지원하는 지표: MMLU-Pro, MMLU, MMMLU, KMMLU, GPQA(-D), HumanEval, MBPP, AIME24/25, MATH, GSM8K, BEIR, NDCG@10, MTEB, IFEval, MT-Bench, LogicKor, Arena Hard, OCRBench(v2), DocVQA, MMMU, Korean Avg, Avg, tok/s, doc/s, pair/s, page/s)
- 빠른 추천 카드를 클릭하면 목록을 가리는 드로어가 열리던 것을, 클릭한 카드 바로 아래에 상세 내용이 펼쳐지는 인라인 아코디언 방식으로 변경. 나머지 카드는 자리 그대로 남고, 펼쳐진 카드는 표 전체 폭으로 확장되어 VRAM 분석·양자화 비교·실행 명령어 등 기존 드로어와 동일한 내용을 보여줌 (내용 생성 로직을 `buildGenerativeDetailBodyHtml`/`buildNonGenerativeDetailBodyHtml`로 분리해 드로어·아코디언이 같은 함수를 재사용). 다른 카드를 클릭하면 이전 카드는 접히고 새 카드가 펼쳐지며, 같은 카드를 다시 클릭하면 접힘. 전체 모델 탐색(전문가) 모드의 드로어/인스펙터 동작은 변경 없음 (별도 상태 변수로 분리)
- 벤치마크 시트 그래프에서 아무 행도 선택하지 않았을 때 빈 화면 대신, 가장 많이 등장하는 지표 상위 3개를 기본으로 보여주도록 변경. 각 지표 그룹 아래에 "OOO 기준 △△△이(가) 가장 높습니다/빠릅니다 (최저 대비 +NN%)" 같은 한 줄 요약 분석을 자동 생성해 추가. 행을 체크하면 여전히 선택한 모델로만 그래프가 바뀜
- 벤치마크 시트(페이지 최하단)에 검색창과 다중 선택 비교 그래프 추가. 모델명·GPU·지표로 실시간 검색해 행을 좁힐 수 있고, 각 행 왼쪽 체크박스로 최대 6개까지 선택하면 선택된 행만 모아 지표(품질 지표명 또는 tok/s·doc/s·pair/s·page/s)별로 그룹핑한 가로 막대 그래프를 표 아래에 렌더링. 서로 다른 지표는 절대 같은 막대 그래프에 섞지 않고, 지표 그룹별로 자체 최대값 기준 상대 막대 길이를 계산. 선택 초기화 버튼으로 비교 상태를 즉시 비움
- 전체 모델 탐색(전문가) 모드에서 "양자화별 추천" 카드 줄이 가로 스크롤로 잘려 보이던 문제 수정: 카드 목록을 `overflow-x: auto` 한 줄 스크롤에서 `flex-wrap`으로 바꿔 화면 폭에 맞게 여러 줄로 자연스럽게 흘러가도록 변경
- 전체 모델 탐색(전문가) 모드의 모델 상세를 넓은 화면(1536px 이상)에서 오른쪽 고정 인스펙터 패널로 전환. 지금까지는 모델을 클릭하면 목록을 가리는 오버레이 드로어가 열렸는데, 이제 넓은 화면에서는 목록 오른쪽에 항상 떠 있는 패널에서 모델을 바꿔가며 VRAM 구성·여유율·속도 근거를 계속 확인할 수 있음. 빠른 추천 모드와 1536px 미만 화면(태블릿·노트북)은 기존 오버레이 드로어 그대로 유지. 아무 모델도 선택하지 않았을 때는 빈 흰 패널 대신 안내 문구를 표시. 목록에 이미 있던 선택 행 강조(파란 왼쪽 선)는 그대로 재사용
- 위 인스펙터 패널을 위해 목록의 "출시/세대"·"권장 설정" 두 열은 인스펙터 패널에서 바로 확인 가능하므로 인스펙터가 열리는 화면 폭에서만 숨김(전체 모델 탐색 목록 자체나 좁은 화면에서는 그대로 노출). 원래 계획한 1280px 기준은 목록의 고정폭 열 10개 합이 이미 850px를 넘어 360~520px 인스펙터와 함께 들어가지 않아 1536px로 조정
- 계산 로직·DOM ID·필터·비교 기능은 변경 없음. jsdom으로 모드 전환 시 body 클래스 토글, 모델 선택 시 인스펙터 내용 갱신과 선택 행 강조, 상세 닫기 시 안내 문구 복귀를 확인

- 생성형 LLM 147종, 임베딩 60종, 리랭커 34종, OCR/VLM 45종 등 등록된 모델 286개 전체에 실제 영문 요약 데이터 추가. 지금까지 영어 모드에서는 모델별 한국어 설명 대신 "language model for {태그}. The Korean description is available in the Korean interface." 같은 일반 문장으로 대체됐는데, 이제 `summary` 필드를 `{ ko, en }` 형태로 바꾸고 기존 한국어 설명을 그대로 번역해 넣어 영어 사용자도 모델별로 실제 설명을 봄. 같은 방식으로 라이선스 정책 31종(고유 요약 22종)의 `summary`도 `{ ko, en }`로 전환해 영어 모드에서 일반 문구 대신 실제 라이선스 조건 설명이 보이도록 함. 데이터 유효성 검사(`npm run check`의 validate-data)와 jsdom 검증으로 전부 객체 형태로 반영됐는지, 언어 전환 시 정상적으로 전환되는지 확인
- GPU를 아직 선택하지 않은 첫 방문 화면을 컴팩트한 드롭다운 한 줄에서 전용 시작 화면으로 교체: RTX 5090/4090/4080 SUPER/4070 Ti SUPER/4060 Ti/3090/3060, Radeon RX 7900 XTX 등 자주 찾는 GPU 카드 8개를 VRAM·대역폭과 함께 큼직하게 보여주고, 그 아래 GPU 프리셋 전체(90개)를 검색해 바로 선택할 수 있는 입력창을 배치. GPU를 고르기 전까지는 하드웨어 설정 패널·결과 목록·벤치마크 시트를 숨겨 화면이 산만해지지 않게 하고, 선택 즉시 기존 계산 화면으로 전환
- GitHub Pages 배포가 `push`로 CI와 동시에 시작되어 CI가 실패해도 이미 배포가 끝나 있을 수 있던 문제 수정: `CI` 워크플로가 main에서 성공적으로 끝난 뒤에만 `workflow_run`으로 배포를 트리거하도록 변경
- 전반적인 UI를 "Light Industrial Benchmark Workbench" 방향으로 재도색: 흰 배경/파란 강조/둥근 카드/pill 배지가 반복되던 기존 톤에서 벗어나, 따뜻한 회백색 캔버스, 하드코딩 없이 `--radius-sm/--radius-md` 토큰으로 통일된 3~5px 모서리, 대부분의 카드 그림자 제거(모달·드로어처럼 실제로 떠 있는 요소만 얇은 그림자 유지)로 정리. 등급 표시는 채워진 pill 대신 점(dot) + 텍스트로, VRAM·속도 등 수치 열은 모노스페이스 고정폭 폰트로 변경
- 모델 상세 패널에 VRAM 메모리 맵(가중치/KV cache/런타임/여유를 하나의 막대로 시각화) 추가. 생성형 LLM과 임베딩·리랭커·OCR/VLM 등 비생성형 모델 상세 모두에 동일하게 적용
- 벤치마크 시트에 근거 출처를 나타내는 짧은 코드 배지(EST 계산 추정 · EXT 외부 공개 참고값 · USER 사용자 측정 · SELF 자체 측정) 추가
- 빠른 추천 카드에서 임베딩·리랭커·OCR/VLM 모델을 추천했을 때 "Ollama 복사" 버튼이 생성형 전용 명령어 생성 함수를 호출해 예외로 죽던 버그 수정. 모델 종류에 맞는 실행 명령어를 생성하도록 분기하고 버튼 라벨도 "실행 명령어 복사"로 일반화
- 영어 UI를 키 기반 메시지(`t(key)`)로 전환하기 시작하고, 재렌더링되는 설정·필터·빠른 추천·벤치마크 문구도 현재 언어로 생성하도록 수정. `?lang=en` 공유 URL과 영어 레이아웃 폭 보정 추가
- 언어 선택 UI를 지구본 아이콘 + 드롭다운에서 헤더 다른 링크들과 어울리는 컴팩트한 `한국어 | EN` 필 토글로 정리
- 영어 모드에서 곳곳이 한국어로 남거나 "Model을", "Search해서 선택"처럼 단어 일부만 바뀌어 깨져 보이던 문제 수정. 번역 치환이 한글 단어 중간을 잘라 붙이지 않도록 경계 검사를 추가하고, 설정 항목 라벨·결과 테이블 헤더·태그·라이선스 문구·추천 사유·벤치마크 시트·비교 모달·고급 도구 안내문 등 번역 누락 범위를 대폭 확충 (약 600개 → 30여 개 잔여, 남은 항목은 GPU 프리셋 설명과 동시 요청/배치 크기 등 일부 드롭다운 옵션 단위 표기 정도)
- 빠른 추천과 모델 상세에 결과 링크 복사 및 GPU·추천 모델·VRAM·추정 속도 요약 카드 PNG 다운로드 추가. 빠른 추천 용도·우선순위도 공유 URL에 저장·복원
- 상세 설정을 `기본 하드웨어 · 보조 GPU · 메모리 보정` 3개 밀집형 그룹으로 재배치하고 입력 높이·간격을 축소. 접힌 고급 도구는 데스크톱에서 2열로 표시하고, 워크로드별 실행 조건도 넓은 화면에서 한 줄 중심으로 정리
- 첫 방문 시 임의의 RTX 4090 결과를 바로 보여주지 않고 GPU를 한 번 명시적으로 선택하도록 변경. 선택한 고정 GPU는 브라우저에 기억하며, 이후에는 빠른 추천을 기본 화면으로 열어 용도·우선순위에 맞는 상위 3개 모델과 상세 계산 진입점을 먼저 표시. GPU 외 VRAM·RAM·동시 요청·고급 도구는 `상세 설정` 안에 유지
- 빠른 추천 모드에서 상단의 상태별 개수(전체/쾌적/잘 돌아감/가능 등) 칩이 계속 보이고 클릭도 됐지만, 이 칩은 전체 모델 탐색 목록의 필터라 빠른 추천에서는 눌러도 화면에 아무 변화가 없어 "안 눌린다"처럼 보이던 문제 수정. 이제 이 칩은 전체 모델 탐색 모드에서만 보이고, 빠른 추천에서 실수로 필터를 걸어둔 채 전체 모델 탐색으로 넘어가 목록이 미리 걸러져 있던 문제도 함께 해결
- GitHub Issue 생성 정책이 `ALL`이고 상호작용 제한이 없음을 확인한 뒤 모델·GPU·벤치마크 제보 링크 재개
- GitHub 저장소 About 설명과 Topics를 LLM·임베딩·리랭커·OCR·VLM·멀티 GPU 범위에 맞게 갱신

## v1.1.0 - AI Workload Expansion - 2026-07-24

- 하드웨어 설정 패널을 페이지 진입 시 기본적으로 펼쳐서 보여주도록 변경 (매번 "설정 변경"을 눌러야 GPU를 바꿀 수 있던 문제)
- "고급 도구"(Hugging Face 직접 계산 · 여러 GPU 배치)를 결과 목록 맨 아래에서 GPU 설정 패널 안으로 이동. 전체 모델 탐색 모드에서 모델 목록(최대 147개 행)을 다 스크롤해야만 닿을 수 있어 사실상 눈에 띄지 않던 문제를 해결
- 페이지 흐름 재구성: 워크로드 탭 위에 `빠른 추천 / 전체 모델 탐색` 모드 전환을 추가해 초보자 모드가 전체 목록 사이에 끼어 흐름을 끊지 않도록 최상위 모드로 분리. Hugging Face 직접 계산·여러 GPU 배치는 결과 목록 아래 "고급 도구" 아코디언으로 이동. 정렬 드롭다운을 검색·필터와 한 줄로 통합
- 모델 비교 모달 레이아웃 정리: 모델별 열 사이 구분선 추가, 비교 제외 버튼을 헤더 우측 상단으로 이동, 상단에 실행 가능 여부 요약 문장 추가, 평가 기준이 다른 대표 공개 평가는 "직접 비교 불가" 표시, 필요 VRAM·추정 속도 차이를 색상으로 강조
- 여러 GPU 배치 계산 결과에 "실행 설정 내보내기" 추가: 배치된 모델 전체의 실행 명령어(.sh)와 Ollama·TEI 기준 docker-compose.yml 초안을 복사/다운로드 가능. OCR/VLM처럼 실행 방식이 모델마다 다른 경우는 compose 자동 생성 대신 안내 주석과 .sh 명령어로 대체
- 초보자 모드(3단계 추천) 추가: 현재 선택된 GPU 기준으로 용도·우선순위 2가지만 고르면 현재 탭에서 실행 가능한 모델 중 상위 3개를 추천 이유와 함께 보여줌. 기존 전체 목록은 그대로 "전문가 모드"로 유지
- 모델 목록에서 체크박스로 최대 3개 모델을 선택해 등급·VRAM·추정 속도·대표 공개 평가·라이선스·태그를 표 형태로 나란히 비교하는 비교 모드 추가 (목록/카드 보기 모두 지원)
- README를 ~180줄의 홍보용 요약 페이지로 축소하고, 계산 수식·정확도/한계·참고 자료를 `docs/methodology.md`, `docs/accuracy-and-limits.md`, `docs/data-sources.md`로 분리
- README를 출시용 요약으로 다시 축소하고 `README.en.md` 및 10초 데모 GIF 추가
- 공개 Issue 생성 제한 복구 확인 전까지 README의 모델·GPU·벤치마크 제보 흐름을 일시 중단으로 표시
- 계산값을 계산 추정·외부 공개 참고값·사용자 측정·자체 측정으로 명확히 구분
- 릴리스 태그에서 GitHub Release를 발행하는 검증 워크플로 추가

- 루프백 전용 로컬 벤치마크 CLI(`scripts/benchmark-cli.mjs`) 추가: Ollama/llama.cpp에 실제 프롬프트를 보내 prefill/decode 속도·TTFT·peak VRAM을 사용자 측정 JSON으로 생성
- Benchmark report 이슈 템플릿에 CLI 출력 JSON을 바로 붙여넣는 칸 추가
- 모델 상세에 동일 조건 사용자/자체 측정값이 있을 때 "예상 vs 측정 · 추정 오차 %"를 표시
- 배치 계산 버튼 옆에 배치된 모델 전체의 공통 병목 기준(동시 접속 인원·처리량) 요약 뱃지 추가
- 기본·보조 GPU와 여러 GPU 배치 인벤토리는 드롭다운을 기본으로 사용하고, 검색 옵션을 선택하면 모델명 자동완성 입력을 표시하도록 변경
- 모델 상세에 동시 처리 용량(베타) 추가: GPU·모델·양자화·컨텍스트 기준 권장/최대 동시 인원과 총·1인당 처리량을 KV cache 기준으로 역산
- 여러 GPU에 모델 동시 배치 추천(베타) 추가: 검색 가능한 체크리스트로 생성형 LLM·임베딩·리랭커·OCR/VLM을 섞어서 선택하면 GPU별 모델·정밀도 배치와 병목 없는 동시 인원·처리량, 배치 불가 모델을 계산
- 모델 라이선스 안내 추가: 상업 이용 가능/조건부/비상업·연구용/원문 확인 필요를 모델별 원문 링크와 함께 표시
- 이기종 GPU 병렬(서로 다른 GPU 두 대의 VRAM·대역폭·연산량 합산, 메모리 분할·통신 손실 반영) 지원 추가
- 요청받은 NVIDIA P102-100 10GB(채굴카드) GPU 프리셋 추가
- 리랭커·LLM·임베딩·OCR/VLM 모델 164종에 출처 연결 공개 품질 벤치마크 수치 추가
- 첫 화면을 카드형 중심에서 목록형 빠른 비교 화면으로 개편
- README Mermaid 다이어그램을 제거하고 자동 양자화 수식 렌더링 오류 수정
- 하드웨어 입력과 고급 설정을 접힘 영역 없이 기본 노출되도록 변경
- 브라우저 보안 제약으로 정확도가 낮은 PC 자동 감지 기능 제거
- 생성형 LLM, 임베딩, 리랭커, 경량 OCR, 문서 VLM, 범용 VLM 탭 추가
- 임베딩 모델 60종, 리랭커 34종, OCR/VLM 모델 45종 추가
- EmbeddingGemma, Jina Embeddings v4, BGE multilingual Gemma2/code/en-ICL, GTE-Qwen2, E5-Mistral, SFR, Granite R2, Stella, Nomic, KURE/KoE5/dragonkue 한국어 임베딩 추가
- bge-reranker-v2.5-gemma2-lightweight, mxbai-rerank v2, gte-reranker-modernbert, Granite reranker R2, Jina v1, BCE, 한국어 리랭커, Sentence Transformers cross-encoder 기준선 추가
- PaddleOCR-VL-1.6, MinerU2.5-Pro, DeepSeek-OCR-2, dots.ocr, dots.mocr, olmOCR-2, DeepSeek-VL2, Qwen3-VL, Qwen2.5-VL, Qwen2-VL, Llama 3.2 Vision, Pixtral, LLaVA-OneVision, Molmo, SmolVLM2, Phi-4 multimodal, Aya Vision, GLM-4.1V, MiniCPM-V, InternVL3.5, Kimi-VL 추가
- 임베딩 encoder FLOPs/activation, 리랭커 query latency, OCR 해상도/megapixel 기반 계산식 추가
- 모든 워크로드에 공통 적용되는 이미 사용 중인 VRAM/안전 여유분 입력과 가용 VRAM 기준 등급 계산 추가
- 컨텍스트, 동시 요청, 출력 토큰, RAG 배치, OCR 배치 페이지에 프리셋과 직접 입력을 함께 제공
- H100/A100 같은 데이터센터 GPU를 빠르게 찾을 수 있도록 GPU 프리셋 검색 입력 추가
- 적합도 요약을 클릭 가능한 필터 칩으로 변경
- 모델 클릭 시 양자화별 비교, VRAM 상세 분석, 실행 방식별 비교, 예시 명령어를 보여주는 상세 패널 추가
- GPU/VRAM/RAM/컨텍스트/필터/선택 모델을 URL 쿼리로 공유할 수 있도록 변경

## v1.0.0 - First public release

- GPU 프리셋 86종
- LLM 모델 114종
- 양자화 옵션 8종
- WebGPU/WebGL 기반 GPU 자동 감지 및 프리셋 추정 매칭
- Ollama, llama.cpp, vLLM, Transformers 조건별 메모리 추정
- 컨텍스트 길이, 동시 요청 수, 평균 출력 토큰, KV cache 정밀도 반영
- GitHub Pages 공개 데모 준비
- 모델/GPU/벤치마크 제보용 Issue template 추가
